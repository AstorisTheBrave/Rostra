## What does this PR do?

<!-- Brief description of the change and why. -->

## Type

- [ ] Bug fix
- [ ] New feature / module
- [ ] Refactor
- [ ] Docs

## Checklist

- [ ] `npm run typecheck` passes
- [ ] `npm run lint` passes
- [ ] `npm run test:env` passes (added/updated tests for new logic)
- [ ] UI is built via `@/ui` (no raw discord.js component builders in modules)
- [ ] Components V2 only (no `EmbedBuilder`), slash-only commands
- [ ] User-facing strings go through `t()`
- [ ] No AI provider/model names anywhere (undercover)
- [ ] Docs updated (`docs/modules/*.mdx` + `docs/sessions/log.mdx`) if structure changed

## How was this tested?

<!-- Commands run, manual steps, etc. -->
