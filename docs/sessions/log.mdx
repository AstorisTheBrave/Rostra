---
last-updated: 2026-06-05
owner: AstorisTheBrave
status: stable
---

## what this is

The running history of structural changes to Rostra, one entry per working session. Read the latest entry
to see where things stand and what was decided.

## log

### 2026-06-09 (stage 1: automod profanity + card fonts)

**changed**: Two live-found bugs. (1) **Automod never caught swears**: the setup-wizard toggle only set the
tenant kill-switch flag, and `/automod setup` omitted `antiProfanity`. Added a feature-provisioner registry
to `services/tenant.ts` (`registerFeatureProvisioner`; `setFeatures` fires it on flag transitions, no
cross-module imports), and automod registers one that applies the recommended filters (now including
profanity) when its toggle flips on and disables when off. `/automod setup` now uses the same `RECOMMENDED`
set. (2) **Image cards rendered tofu boxes** on hosts with no system fonts (Ori): added bundled DejaVu Sans
(`assets/fonts/`), a shared `src/utils/cardFont.ts` (`ensureCardFonts` lazy-registers via GlobalFonts;
`cardText` NFKC-folds styled-unicode names like 𝐬𝐲𝐧𝐜 -> sync and strips zero-width/control chars), wired into
all four renderers (profile, leveling, welcome, roleplay ship). Test: `cardFont.test.ts`. Biome + tsc clean.

**files touched**: `src/services/tenant.ts`, `src/modules/automod/index.ts`, `src/utils/cardFont.ts` (+test),
`src/modules/{profile,leveling,welcome}/card.ts`, `src/modules/roleplay/shipCard.ts`, `assets/fonts/*.ttf`.

### 2026-06-08 (panel hosting: launcher fixes + detailed Ori tutorial)

**changed**: Hardened `panel-start.mjs` after live testing on Ori Host. The `--import tsx` loader does NOT
read tsconfig `paths`, so `@/` failed; fixed by running the cluster through the tsx CLI (`npx tsx
src/cluster.ts`) with `TSX_TSCONFIG_PATH` pinned to the repo tsconfig, which the native shards inherit. Root
cause of the persistent crash was operator config (MAIN_FILE pointed at `src/cluster.ts` instead of
`panel-start.mjs`, so the launcher never ran, identifiable by the absence of Prisma output). Expanded
[[Hosting on Ori Host]] into a full tutorial: prerequisites (Neon + Upstash + read-only GitHub PAT), exact
Startup-tab fields, `.env` via Files tab, a "healthy boot" sequence (Prisma output = launcher is running),
and a troubleshooting table mapping every real error string (including the misleading "MAIN_FILE is not set"
red herring the egg prints on any non-zero exit). No em dashes.

**files touched**: `panel-start.mjs`, `docs/wiki/Hosting-on-Ori-Host.md`, `docs/sessions/log.mdx`.

### 2026-06-08 (panel hosting: panel-start.mjs launcher + Ori Host wiki)

**changed**: Added support for game-panel hosts (Pterodactyl / Ori Host) that run `node <MAIN_FILE>` against
a raw clone. New root launcher `panel-start.mjs` solves the three failure modes a user hit on Ori: (1) plain
`node` cannot run `.ts`/`@/` aliases, so it registers tsx then imports `src/cluster.ts` (native shards
inherit tsx via the manager's `execArgv: ["--import","tsx"]`, confirmed in cluster.ts); (2) panels expose no
secret env UI, so it loads an uploaded `.env` via `process.loadEnvFile` (Node 20.12+, no dep added); (3)
panels block npm install scripts, so it runs `prisma generate` + `prisma migrate deploy` before boot. Added
`start:panel` npm script. NOT used by Docker/VPS (those keep `npm start` = `tsx src/cluster.ts`). Biome + tsc
clean. New wiki page [[Hosting on Ori Host]] (set MAIN_FILE=panel-start.mjs, external Neon+Upstash, .env via
Files tab, error->fix table mapping the exact crash strings), linked from Home, _Sidebar, Hosting Rostra,
Easy Hosting. No em dashes.

**files touched**: `panel-start.mjs` (new), `package.json`, `docs/wiki/{Hosting-on-Ori-Host,Home,_Sidebar,
Hosting-Rostra,Easy-Hosting}.md`, `docs/sessions/log.mdx`.

### 2026-06-08 (wiki: reputation guide + glossary; verified module coverage)

**changed**: Audited the 8 modules missing from the CLAUDE.md room index (`highlight, role/bulkrole,
voicerole, poll, rep/reputation, serverstats, suggest, sticky`) against the wiki. Finding: 7 were already
documented (highlight/sticky/serverstats in Utility and Extras; poll/suggest in Engagement; voicerole/role in
Roles and Self-Roles; all in Commands). Only `/rep` had no feature guide, so added a **Reputation** section to
`Engagement.md` (`/rep give|view|leaderboard`, validated against source). Added a new non-technical
[[Glossary]] page (token, intent, shard, VPS, Redis, etc.) and linked it from Home, _Sidebar, and Using
Rostra. Note for future: the 8 modules are still absent from the `docs/` room index and have no
`docs/modules/*.mdx` (separate dev-doc task). No em dashes.

**files touched**: `docs/wiki/{Engagement,Glossary,Home,_Sidebar,Using-Rostra}.md`, `docs/sessions/log.mdx`.

### 2026-06-08 (wiki: two-track restructure + managed hosting + referrals)

**changed**: Reorganized the wiki into two audience tracks and added managed hosting coverage. (1) New hub
pages [[Using Rostra]] (no-code user track) and [[Hosting Rostra]] (technical operator/dev track) act as
tutorial indexes. (2) New [[Easy Hosting]] page: click-and-go managed hosting (Railway, PebbleHost, Fly.io)
with real per-host steps and referral note. (3) New [[Hosting at Scale]] page: native sharding on a VPS
(Hetzner/DigitalOcean/Vultr) with self-run or managed Postgres/Redis (Neon/Upstash), tied to `SHARDING_MODE`
/ `TOTAL_SHARDS` / `SHARDS_PER_CLUSTER`. (4) Rewrote `Home.md` to a "pick your path" fork and regrouped
`_Sidebar.md` (Start / Using the bot / Hosting the bot / Operations / Contributing). All command names in new
pages validated against real `.setName()` in `src/modules/*` (e.g. `/setup wizard` confirmed). No em dashes.
Referral links left as a maintainer task: operator fetches own codes (Railway 15%, PebbleHost 70% first
invoice, DO/Vultr CPA; Hetzner program ends 31 Aug 2026; Fly.io has none).

**files touched**: `docs/wiki/{Home,_Sidebar,Using-Rostra,Hosting-Rostra,Easy-Hosting,Hosting-at-Scale}.md`,
`docs/sessions/log.mdx`.

### 2026-06-08 (ops: CI fix, security review, wiki + discussions)

**changed**: Repo health pass. (1) **CI was red on main**: the undercover guard caught `gpt-4o-mini` and
`OpenAI-compatible` in `i18n-draft.ts`; replaced with a required `TRANSLATE_MODEL` (no model-name default)
and neutral "chat-completions provider" wording. CI green again (667582a). (2) **Security**: `npm audit` 0;
Dependabot alerts 0; two medium CodeQL alerts (`js/file-access-to-http`, `js/http-to-file-access`) in the
`i18n-draft` dev tool dismissed as won't-fix with justification (controlled allowlist paths, dev-only
tooling, operator-configured endpoint - the file/HTTP dataflow is the tool's purpose). 0 open alerts. (3)
**PRs**: 3 Dependabot CI-action bumps (codeql-action 3->4, setup-node 4->6, metadata-action 5->6) refreshed
with `@dependabot rebase` for the maintainer to merge; release-please `0.1.2` PR left for a release-timing
decision. (4) **Discussions**: enabled; created the Welcome announcement (#20). (5) **Wiki**: enabled but
needs a one-time UI seed before git push works (no API for the first page), so the 7 pages live in
`docs/wiki/` (Home, Getting-Started, Commands, Localization, Hybrid-Sharding, Feature-Flags, Deploying) with
`_PUBLISH.md` giving the one-time publish steps. No em dashes in any user-facing text.

**files touched**: `src/scripts/i18n-draft.ts`, `docs/sessions/log.mdx`, new `docs/wiki/*.md`.

**verified**: CI green; undercover guard passes locally; 0 security alerts; typecheck/lint/tests unaffected
(228 tests).

### 2026-06-08 (scaling: hybrid clustering wired behind SHARDING_MODE=hybrid)

**changed**: Wired `discord-hybrid-sharding` (^3.0.1, 0 vulns) so the fleet can scale across many cluster
processes/hosts, opt-in via `SHARDING_MODE=hybrid` (default stays native; no feature-code changes). Real
`HybridIpc` in `src/cluster/ipc.ts` over the `ClusterClient` bridge (cross-cluster `broadcastEval`);
`createIpc` picks it by mode. `BotClient` sets `shards`/`shardCount` from `getInfo()` and attaches a
`ClusterClient` in hybrid mode. New `src/cluster/hybrid.ts` (`runHybridManager`: ClusterManager spawn + web
server + autopost + command sync + leader-gated crons); `cluster.ts` delegates to it when hybrid. New
cross-cluster **identify queue** `src/services/identify.ts` (Redis lock serialising cluster logins across
hosts to respect Discord's identify concurrency; released on `ready`), gated in `bot.ts` for hybrid. New
config `SHARDS_PER_CLUSTER`. README gained a Sharding-and-clustering section; new tutorial
`docs/infrastructure/hybrid-sharding.mdx` (setup, env vars, single vs multi host, live-change vs deploy).
`.env.example` updated. No em dashes in the user-facing docs.

**files touched**: `src/cluster/{ipc,hybrid}.ts`, `src/cluster.ts`, `src/client/BotClient.ts`, `src/bot.ts`,
`src/services/identify.ts`, `src/config.ts`, `package.json` (+ dep), README, `.env.example`,
`docs/infrastructure/hybrid-sharding.mdx`.

**verified**: typecheck clean; full suite 228/228 (native path unchanged, default); biome clean. Per the
plan, the hybrid path is wired + type-checked but needs a real multi-process/multi-host deploy to
smoke-test before production (same caveat as the Lavalink music path). The whole scaling plan is now built:
control bus, live i18n overlay, feature flags/kill-switches, leader election, translate-everything pipeline,
and hybrid clustering.

### 2026-06-08 (scaling: leader election for single-instance jobs + scaling runbook)

**changed**: New `src/services/leader.ts` - Redis leader election so the manager-process crons (feeds,
server-stats, autopost, command sync) run on exactly ONE cluster manager across the fleet, not once per
cluster (a duplicate-execution bug once hybrid clustering lands). `acquireLeadership` holds a renewing TTL
lease; `startWhenLeader` starts the work when this instance wins and keeps polling so a standby takes over
if the leader dies; `releaseLeadership` on shutdown. `cluster.ts` now gates `registerBuiltinCron`/`startCron`
on `startWhenLeader("manager-crons", …)` - a trivial yes in native single-manager mode, correct under
clustering. New `docs/infrastructure/scaling.mdx` capturing the locked decisions (hybrid clustering,
blue-green deploys, AI drafts, dormant flags), shard/identify math, the data-vs-code live-change split, and
the blue-green deploy runbook.

**files touched**: new `src/services/leader.ts`, `docs/infrastructure/scaling.mdx`; `src/cluster.ts`.

**verified**: typecheck clean; biome clean; full suite still 228/228 (no test changes). Leader lock is
forward-compatible and harmless in native mode.

**remaining scaling work (needs a live multi-process/host env to smoke-test, like the Lavalink path)**:
wire `discord-hybrid-sharding`'s `ClusterManager` + a real `HybridIpc` behind `SHARDING_MODE=hybrid`, plus
a cross-cluster identify queue. Everything else (control bus, live i18n overlay, feature flags/kill-switches,
leader election, translate-everything pipeline) is built and verified.

### 2026-06-08 (i18n: translate-everything pipeline - AI drafts + validation gate)

**changed**: The "translate everything (16 langs)" pipeline (approved: AI drafts + platform review). New
pure `src/i18n/validate.ts` (`validateTranslation`: every translated string must compile as ICU and keep
the same simple `{placeholder}` set as English; `simpleArgs`) + `npm run i18n:validate` CI gate that fails
on broken translations (missing keys are fine - English fallback). New `npm run i18n:draft`
(`src/scripts/i18n-draft.ts`): machine-translates every missing key for all 16 langs via a configurable
chat-completions provider (`TRANSLATE_*` env added to `config.ts`, neutral names), glossary- and
ICU-aware, **validates each draft before writing**, never overwrites existing translations, and records
drafted keys in `locales/<code>/_drafts.json` for review. Flow: `i18n:draft` -> review -> `i18n:bundle` ->
`i18n:push` (live, no restart). Ran the validator on all current translations: clean.

**files touched**: new `src/i18n/{validate,validate.test}.ts`, `src/scripts/{i18n-validate,i18n-draft}.ts`;
`src/config.ts` (+ `TRANSLATE_*`), `package.json`, `docs/modules/localization.mdx`.

**verified**: typecheck clean; validate tests 5/5; full suite 228/228; biome clean; `i18n:validate` passes.
(Draft script builds + typechecks; needs a provider key to run, which the maintainer supplies.)

### 2026-06-08 (scaling: dormant-by-default feature flags + kill-switches)

**changed**: First piece of the 100K->1M scaling plan (approved: hybrid clustering, blue-green deploys, AI
drafts + review, dormant-by-default flags). Built the **global feature-flag framework** on the control bus
so features ship dormant in a release and activate live, and any feature is killable in seconds fleet-wide
with no restart. New `src/services/featureFlags.ts`: `registerFeature(name, default=false)` (declare at
module import), `isFeatureLive(name)` (override -> default -> false), `setFeatureLive` (persist + apply +
publish on `rostra:flags`), `loadFeatureFlags`/`subscribeFeatureFlags` (wired into `BotClient.init`). New
`FeatureFlag` model. New owner-only **`owner` module**: `/owner feature list|set <name> <enabled>` (gated on
`config.discord.ownerIds`, autocompletes declared flags). Demonstrated as a real kill-switch: the `ai`
module registers `ai` (default on) and `/ask` checks `isFeatureLive("ai")`, so a degrading upstream can be
cut fleet-wide instantly (`/owner feature set ai off`). Pure flag logic tested.

**files touched**: new `src/services/{featureFlags,featureFlags.test}.ts`, `src/modules/owner/index.ts`;
`src/modules/ai/index.ts`, `src/client/BotClient.ts`, `prisma/schema.prisma` + `0_init`, CLAUDE.md.

**verified**: typecheck clean; flag tests 5/5; full suite 223/223; biome clean; baseline has `FeatureFlag`.
Next scaling pieces: i18n:draft + ICU/placeholder validator, hybrid clustering (HybridIpc), leader-elected
manager jobs + blue-green health/runbook.

### 2026-06-08 (zero-restart hot reload: control bus + live translation overlay)

**changed**: Made translations (and any future "reload this" signal) hot-loadable across the whole shard
fleet with **no restart** - the strategic answer to changing things on a running bot at millions-of-users
scale. New generic **Redis control bus** `src/services/bus.ts` (`publish`/`subscribe`/`closeBus`; the
subscriber duplicates the cache's ioredis client; no-op without Redis). New **live translation overlay**
`src/i18n/live.ts`: `LocaleString` DB rows overlay the bundled file baseline, loaded at boot
(`loadLiveLocales`); `setLocaleString`/`pushLocaleBundle` persist + apply locally + publish on channel
`rostra:i18n`; every shard's `subscribeLocaleReload` re-applies in-memory instantly. New `npm run i18n:push`
(`src/scripts/i18n-push.ts`) promotes the file translations into the live store and fans out one reload -
so "add a language" is a data push, not a deploy. Wired `loadLiveLocales` + `subscribeLocaleReload` into
`BotClient.init`, and `closeBus` into shutdown. New `LocaleString` model. Pure `groupRows` tested.

**files touched**: new `src/services/bus.ts`, `src/i18n/{live,live.test}.ts`, `src/scripts/i18n-push.ts`;
`src/client/BotClient.ts`, `src/bot.ts`, `prisma/schema.prisma` + `0_init`, `package.json`,
`docs/modules/localization.mdx`.

**verified**: typecheck clean; full suite 218/218 (groupRows 2/2); biome clean; baseline includes
`LocaleString`. (Fixed a self-inflicted bug: the row-key separator was a stray null byte - replaced the
string-joined map key with an explicit array grouping, now tested.) Net: translations are editable/addable
live, fleet-wide, with zero downtime.

### 2026-06-08 (localization - translation pipeline + wave-1 content)

**changed**: Made the translation content pipeline scalable and added real wave-1 content. New
`src/scripts/i18n-bundle.ts` (`npm run i18n:bundle`) scans `locales/<code>/*.json` and **regenerates**
`src/i18n/bundled.ts` (static imports, bundler-safe) - adding a translation is now drop-a-file + run-script,
no hand-editing. New `src/scripts/i18n-coverage.ts` (`npm run i18n:coverage`) reports each language's
completeness vs the English source (module `i18n` + common) and lists missing namespaces. Translated
`core`, `preferences`, and `moderation` DM strings into all six wave-1 languages (fr/de/es/pt-BR/ru/zh-CN),
so the help panel, `/preferences`, and the moderation target-DM showcase render natively in those languages
(wave-1 coverage 1% -> 7%, 35/493 keys each). The rest falls back to English; the coverage report guides the
remaining content (community/AI).

**files touched**: new `src/scripts/i18n-{bundle,coverage}.ts`; `src/i18n/bundled.ts` (now generated);
`src/i18n/locales/{fr,de,es,pt-BR,ru,zh-CN}/{core,preferences,moderation}.json`; `package.json` (scripts);
`docs/modules/localization.mdx`.

**verified**: bundle regenerates to 24 registrations; typecheck clean; i18n tests 9/9; translations resolve
(spot-checked fr/ru/zh-CN); full suite 216/216; biome clean. Remaining localization work is purely
translation content via `i18n:coverage` + the file-drop workflow.

### 2026-06-08 (localization - translations + native command metadata; platform complete)

**changed**: Finished the localization platform. (5) **Wave-1 translation files**: `locales/<code>/
common.json` for fr/de/es/pt-BR/ru/zh-CN (the shared error/ping strings), statically imported + registered
via `src/i18n/bundled.ts` (bundler-safe; English fallback for anything untranslated). Added
`locales/_glossary.json` (do-not-translate brand/feature terms + ICU placeholder rules for community/AI
translation). Registration wired into the commands loader. (6) **Native command-metadata localization**:
`commandLocalizations.ts` `applyCommandLocalizations` merges per-locale `description_localizations` (keyed by
Discord locale code from `SUPPORTED_LOCALES`) into the command payload in `collectCommandJSON`, so the slash
list shows localized in each client; seed descriptions for `help`/`preferences` in the 6 wave-1 langs. New
`docs/modules/localization.mdx`.

**files touched**: `src/i18n/{bundled,commandLocalizations}.ts` + `locales/{fr,de,es,pt-BR,ru,zh-CN}/
common.json` + `locales/_glossary.json`, `src/i18n/index.test.ts`, `src/client/loaders/commands.ts`,
`docs/modules/localization.mdx`.

**verified**: typecheck clean; i18n tests 9/9 (incl. bundled fr/de resolution + `normalizeLocale`); full
suite 216/216; biome clean. **Localization platform is complete**: ICU engine, ambient + per-recipient
locales, UserPreference service, pipeline wiring, `/preferences` + `/setup language` + `/help` switch, fun
opt-outs, wave-1 translations, and native command metadata. Remaining is translation *content* (waves +
Crowdin/AI), not engineering.

### 2026-06-08 (localization - user surfaces: /preferences, /setup language, /help switch, opt-outs)

**changed**: The user-facing half of the localization platform. New **`preferences` module**: `/preferences
language <lang>` (16 langs + Auto), `/preferences feature <name> <on|off>` (opt in/out of fun /
dmNotifications / profilePublic / reputationPublic / matchmaking / achievementNotifications), `/preferences
view`; DM-capable, not guild-gated. **`/setup language <lang>`**: converted `/setup` to subcommands
(`wizard` = the old panel, `language` = guild language via `setGuildLocale`). **`/help` live language
switch**: added a language select to the help panel; choosing one calls `setUserLocale` and re-renders the
panel in the new language (within `runWithLocale`). **Fun opt-out enforcement**: `/roleplay` (ship + all
targeted gif actions) and `/games tictactoe` now refuse when the **target** has opted out of `fun`. Added
`preferences` to the `/help` utility category + README.

**files touched**: new `src/modules/preferences/index.ts`; `src/modules/setup/index.ts`,
`src/modules/core/{index,categories}.ts`, `src/modules/roleplay/index.ts`, `src/modules/games/index.ts`,
README.

**verified**: typecheck clean; full suite 214/214; biome clean. Note: `/setup` is now `/setup wizard` +
`/setup language` (subcommand split). Still to come: translated locale files (wave 1) + native Discord
command-metadata localization.

### 2026-06-08 (localization platform - core: ICU engine + service + pipeline)

**changed**: Landed the core of the localization platform (per the approved design plan; decisions: ICU +
our layer, ambient `AsyncLocalStorage` + explicit translators for multi-recipient, wave-1 langs, `/setup`
+ `/preferences`). (1) **i18n engine** rewritten: `t()` now formats with **ICU MessageFormat**
(`intl-messageformat`, 0 vulns) so `{count, plural, ...}`/`select`/`number`/`date` work across all 16 langs
via `Intl.PluralRules`; malformed ICU falls back to plain `{var}` interpolation (no throw). Locale
precedence: explicit arg -> ambient (`runWithLocale`) -> English. New `getTranslator(locale)`,
`runWithLocale`, `getAmbientLocale`. New `src/i18n/locales.ts`: `SUPPORTED_LOCALES` (16, with Discord
codes + RTL + wave), `normalizeLocale` (maps Discord/regional codes to ours). (2) **Localization service**
`src/services/localization.ts` (peer to tenant): new `UserPreference` model (locale + feature opt-outs),
L1/L2 cache like tenant, `resolveLocale({user,guild,interactionLocale,scope})` (user/actor: pref -> Discord
client locale -> guild -> en; guild: guild -> en), `getUserPreference`/`setUserLocale`/`setUserFeature`/
`isOptedOut`, `setGuildLocale`, and `localizeAction({actor,target,guild})` -> `{actorT,targetT,guildT}`. (3)
**Pipeline wired**: `runCommand` + the component router resolve the actor's locale and run within
`runWithLocale`, so every `t()` in a handler renders in the user's language with zero call-site churn. (4)
**Multi-audience showcase**: `/mod warn|timeout|ban|kick` now DM the target in **the target's** language
(`notifyTarget`, respects the `dmNotifications` opt-out) while the moderator's confirmation stays in the
moderator's language. New `moderation:dm.*` strings.

**files touched**: `src/i18n/{index,locales,index.test}.ts`, `src/services/localization.ts`,
`src/pipeline/runCommand.ts`, `src/interactions/router.ts`, `src/modules/moderation/index.ts`,
`prisma/schema.prisma` + `0_init`, `package.json` (+ `intl-messageformat`).

**verified**: typecheck clean; i18n engine tests 7/7 (ICU plural, explicit/ambient locale, fallback); full
suite 214/214; biome clean; baseline includes `UserPreference`. Still to come: `/preferences` + `/setup
language` + `/help` switch + opt-out enforcement (ship/fight), translated locale files, native command
metadata localization.

### 2026-06-07 (starboard: full engine - blacklists, downvotes, filters, tiers, overrides)

**changed**: Completed the deepest Starboard-bot layer on top of the multi-board base, so starboard now has
feature parity with the headline systems. New pure `eligibility.ts` (all tested in `eligibility.test.ts`):
`isAllowed` (blacklist/whitelist with user>role>channel specificity; more-specific whitelist beats
less-specific blacklist), `passesFilters` (min length / min attachments / require image / max message age /
NSFW-channel), `netStars` (valid upvoters minus downvoters, floored at 0, honouring self/bot/blacklist),
`tierEmoji`/`parseDisplayTiers` (display tiers), and `resolveSettings` (override inheritance: role then
channel overrides merge over the board base, channel wins). New `StarboardOverride` model + many new
`Starboard` columns (blacklist/whitelist user/role/channel arrays, `downvoteEmojis`, `removeInvalid`,
`minChars`, `minAttachments`, `requireImage`, `maxMessageAgeHours`, `requireNsfwChannel`, `displayTiers`
JSON). Events `processBoard` rewritten around the engine: net star counting from all of a message's
reactions, filter gate, per-context override resolution, tiered header emoji, and remove-invalid-reactions
on add. New commands: `/starboard block|allow` (toggle black/whitelist), the `override create|set|delete`
group, and ~9 more `edit` options. Boards are now loaded `include: { overrides }` and cached.

**files touched**: `src/modules/starboard/{index,events,service,eligibility,eligibility.test}.ts`,
`prisma/schema.prisma` + `0_init`, `docs/modules/starboard.mdx`.

**verified**: typecheck clean; starboard tests 16/16 (6 new engine cases); full suite 210/210; biome clean;
baseline includes `StarboardOverride` + all new columns. Starboard is now feature-complete vs the docs;
nothing left flagged as unbuilt.

### 2026-06-07 (modmail cross-shard fix + shard audit)

**changed**: Fixed modmail so it works at any shard count (e.g. 78 shards). DMs arrive on shard 0;
`findModmailGuild` (which only scanned the local shard's cache) is replaced by `findModmailTarget`, which
`broadcastEval`s every shard for "do you own a modmail-enabled guild this user is in?" and returns
`{guildId, shardId}`. Relaying happens on the owning shard: directly if local, else via a targeted
`broadcastEval` into that shard's registered `globalThis.__rostraModmail.relay`. Extracted
`relayUserMessage` (open/continue thread + post) and `enabledModmailGuildIds`; `createThread` now takes a
plain `{id,tag,username}` so it works without a cross-shard `User` object.

**shard audit**: reviewed every cross-guild access for 78-shard safety. Findings: stats (`core/stats.ts`)
and the manager already aggregate via `broadcastEval` (correct); per-guild in-memory state (security raid
windows, automod spam buckets, extras afk/snipe, etc.) is correct because a guild is always owned by one
shard; scheduler re-arms per-shard by guild ownership; birthday `runDaily` runs on every shard but acts
only on owned guilds (each birthday handled once); crons (feeds/serverstats) run in the manager via REST.
Modmail was the only genuine break.

**files touched**: `src/modules/modmail/{events,service}.ts`, `docs/modules/modmail.mdx`.

**verified**: typecheck clean; modmail tests 3/3; full suite 204/204; biome clean.

### 2026-06-07 (full multi-board starboard rebuild)

**changed**: Rebuilt `starboard` from a single per-guild config into a full **multi-board** system, per the
maintainer's request and the official Starboard docs (re-cloned to `Archive/starboard-docs`, gitignored).
Replaced `StarboardConfig` with a `Starboard` table (many per guild: own channel, `emojis[]`,
`requiredStars`, `removeStars` hysteresis, `selfStar`, `filterBots`, `syncDeletes`, `rewardRoleId`/
`rewardStars`, `authorRoleId` for role-based boards, `ignoredChannels`, `enabled`); re-keyed
`StarboardEntry` to `[starboardId, messageId]` so one message can sit on several boards; added
`AutostarChannel` (bot pre-reacts to new messages, no auto-post). Reaction engine now routes each star to
every matching board via `boardsForEmoji` and processes per board. New command surface: `/starboard
create|delete|list|edit|ignore|leaderboard` + `autostar add|remove|list` (was channel/threshold/emoji/...).
Pure helpers `parseEmojiList` (multi-emoji) added; `effectiveStarCount` param `ignoreBots` -> `filterBots`.
No external consumers of the old models (profile stats does not touch starboard), so the swap was clean;
baseline `0_init` regenerated.

**files touched**: `src/modules/starboard/{index,events,service,service.test}.ts`, `prisma/schema.prisma` +
`0_init`, `docs/modules/starboard.mdx`, CLAUDE.md room index.

**verified**: typecheck clean; starboard tests 10/10 (emoji parse/list, reactionKey, display,
effectiveStarCount, decide); full suite 204/204; biome clean; baseline has `Starboard`/`StarboardEntry`/
`AutostarChannel`.

**still not built** (documented in starboard.mdx): the deepest Starboard-bot layer - override-inheritance
engine, blacklists/whitelists, message-filter groups + display tiers, downvote emojis. The multi-board +
per-board settings + autostar + reward + leaderboard core is complete.

### 2026-06-07 (giveaway entry requirements) - backlog item 17 part 2/2

**changed**: Extended `giveaways` (second half of item 17) with entry requirements. `/giveaway start` gained
`required_role`, `required_level`, and `min_account_days` options; the enter button now runs a pure
`checkEntry` (role -> level -> account-age, first failure wins) before counting an entry, replying with the
missing requirement otherwise. Requirements are shown on the giveaway message. New `Giveaway` columns
`reqRoleId`, `reqLevel`, `reqAccountDays`. Level is read via `getMemberLevel` (queries `LevelUser.level`
through the shared Prisma client - no cross-module import, so module isolation holds).

**files touched**: `src/modules/giveaways/{index,service,service.test}.ts`, `prisma/schema.prisma` +
`0_init`, `docs/modules/giveaways.mdx`.

**verified**: typecheck clean; giveaway tests 7/7 (`checkEntry` role/level/age + existing `pickWinners`);
biome clean; baseline includes the three new columns.

**item 17 status**: logging depth + giveaway depth shipped. The third sub-area (music filters / autoplay /
playlists) is intentionally NOT shipped: it needs a live Lavalink node to smoke-test (untestable in CI), and
shipping unrunnable audio code would violate the verify-before-ship rule. Left as the one documented
infra-dependent remainder.

### 2026-06-07 (logging depth) - backlog item 17 part 1/2

**changed**: Extended `logging` (first half of the final backlog item 17) with three new event types:
`messageDeleteBulk` (bulk delete count), `voiceStateUpdate` (join/leave/move between voice channels), and
`guildMemberUpdate` nickname changes. New `LoggingConfig` toggle columns `bulkDelete`, `voiceMoves`,
`nicknameChanges` (default on), added to the `LogEvent` union and the `/logging toggle` + `status` event
list. Uses existing intents (GuildMessages/GuildVoiceStates/GuildMembers).

**files touched**: `src/modules/logging/{index,events,service}.ts`, `prisma/schema.prisma` + `0_init`,
`docs/modules/logging.mdx`.

**verified**: typecheck clean; biome clean; full suite still green; baseline includes the three new columns.

### 2026-06-07 (suggestions) - backlog item 16 part 3/3 (item 16 COMPLETE)

**changed**: New `suggest` module (final part of item 16). `/suggest add` posts a numbered card to a
configured channel with 👍/👎 toggle-vote buttons; `/suggest setup` (Manage Server) sets the channel +
flips the `suggestions` flag; `/suggest approve|deny <number> [reason]` (Manage Server) updates status and
re-renders the card. The `sug` component handler edits in place via `interaction.update`; pressing the same
vote side clears it, the opposite flips it. New models `SuggestionConfig`, `Suggestion` (per-guild
`number`), `SuggestionVote`, `GuildSuggestionCounter`. Wired into `/setup`, emojis (💡 `suggest`), `/help`
community category, README. This completes backlog item 16 (counting + polls + suggestions).

**files touched**: new `src/modules/suggest/{index,service,service.test}.ts`,
`src/modules/setup/index.ts`, `src/ui/emojis.ts`, `src/modules/core/categories.ts`,
`prisma/schema.prisma` + `0_init`, `docs/modules/suggest.mdx`, README.

**verified**: typecheck clean; suggest tests 5/5 (vote toggle/tally logic); biome clean; baseline includes
the four new tables.

### 2026-06-07 (button polls) - backlog item 16 part 2/3

**changed**: New `poll` module (second of three for item 16). `/poll create <question> <option1> <option2>
[option3..5]` posts a Components V2 message with one button per option + an End button; votes are DB-backed
(`Poll` + `PollVote`, one changeable vote per user). The `poll` component handler edits the message in place
via `interaction.update` (fast enough that safeAck never auto-defers): `poll:vote:<id>:<i>` re-tallies,
`poll:end:<id>` (creator only) closes + re-renders without buttons. Pure `resultBar` tested. Added `/poll`
to the `/help` community category + README; no tenant flag (stateless).

**files touched**: new `src/modules/poll/{index,service,service.test}.ts`,
`src/modules/core/categories.ts`, `prisma/schema.prisma` + `0_init`, `docs/modules/poll.mdx`, README.

**verified**: typecheck clean; poll tests 3/3 (`resultBar`); biome clean; baseline includes `Poll` +
`PollVote`. Suggestions (item 16 part 3/3) still to come.

### 2026-06-07 (counting game) - backlog item 16 part 1/3

**changed**: New `counting` module (first of three for backlog item 16). A designated channel where members
count up; `messageCreate` validates via pure `parseCount` + `evaluateCount` (next = current+1, no
consecutive same-user), reacts ✅/🏆 or resets to 1 with ❌ on a mistake. New `CountingConfig` model (keyed
by channelId, tracks `current`/`best`/`lastUserId`). `/counting setup|disable|status`. Wired the `counting`
feature into `/setup`, `src/ui/emojis.ts` (🔢), and the `/help` community category + README. Polls and
suggestions (the other two parts of item 16) still to come.

**files touched**: new `src/modules/counting/{index,events,service,service.test}.ts`,
`src/modules/setup/index.ts`, `src/ui/emojis.ts`, `src/modules/core/categories.ts`,
`prisma/schema.prisma` + `0_init`, `docs/modules/counting.mdx`, README.

**verified**: typecheck clean; counting tests 6/6; biome clean; baseline includes `CountingConfig`.

### 2026-06-07 (economy depth: shop, items, buyable roles)

**changed**: Extended `economy` (backlog item 15) with a shop. New `shop.ts`: catalog CRUD
(`listShop`/`getItem`/`upsertItem`/`removeItem`), inventory (`getInventory`/`consumeItem`), and a pure
`evaluatePurchase(item, wallet, qty)` (role items force qty 1; checks stock + wallet) that `buyItem`
mirrors so the rules live in one tested place. Buyable roles (item with `roleId`) are granted on purchase
and refunded if the grant fails (`refundPurchase`); other items go to inventory and are consumed by `use`.
New subcommands `/economy shop|buy|inventory|use` + Manage-Server `additem|removeitem`. New models
`ShopItem` + `InventoryItem`.

**files touched**: new `src/modules/economy/{shop,shop.test}.ts`, `src/modules/economy/index.ts`,
`prisma/schema.prisma` + `0_init`, `docs/modules/economy.mdx`.

**verified**: typecheck clean; economy shop tests 5/5 (`evaluatePurchase` cost/stock/role/wallet); biome
clean; baseline includes the two new tables. (Economy is already in `/setup` + `/help` + emoji.)

### 2026-06-07 (image cards: rank + welcome)

**changed**: Added two `@napi-rs/canvas` image cards (backlog item 14). **Leveling rank card**
(`leveling/card.ts`, `renderRankCard`): `/level rank` now sends an 800x240 PNG (avatar, level, rank, XP
progress bar) instead of text; new `rankPosition(guildId, userId)` service helper (Redis ZREVRANK, Prisma
count fallback). **Welcome card** (`welcome/card.ts`, `renderWelcomeCard`): optional 800x280 greeting
image attached on join when the new `WelcomeConfig.welcomeCard` flag is on; toggle via `/welcome card
<bool>`; render failure falls back to text. Both mirror `profile/card.ts` (AttachmentBuilder + Components
V2 gallery). Removed the old ASCII `progressBar` from leveling (rank card replaces it).

**files touched**: new `src/modules/leveling/card.ts`, `src/modules/welcome/card.ts`;
`src/modules/leveling/{index,service}.ts`, `src/modules/welcome/{index,events}.ts`,
`prisma/schema.prisma` + `0_init`, `docs/modules/{leveling,welcome}.mdx`.

**verified**: typecheck clean; full suite 179/179 (no new pure logic to unit-test; canvas rendering is not
headless-testable, consistent with profile/card.ts); biome clean; baseline includes `welcomeCard`.

### 2026-06-07 (modmail)

**changed**: New `modmail` module (backlog item 13) - DM-to-staff threads. Members DM the bot; the first
mutual guild (on this shard) with modmail enabled opens/continues a public thread in a staff channel via
`createThread`, and the DM is relayed in. Staff replies in the thread relay back to the user's DMs, with
`//`-prefixed lines kept internal (`isStaffNote`). `relayBody` formats labelled lines + attachment URLs
(both pure, tested). New models `ModmailConfig` + `ModmailThread`; `/modmail setup|disable|status|close`.
Added `GatewayIntentBits.DirectMessages` to `BotClient` (Partials.Channel/Message already present). Wired
the `modmail` feature into `/setup` FEATURES, `src/ui/emojis.ts` (📬), and `/help` management category.

**files touched**: new `src/modules/modmail/{index,events,service,service.test}.ts`,
`src/client/BotClient.ts`, `src/modules/setup/index.ts`, `src/ui/emojis.ts`,
`src/modules/core/categories.ts`, `prisma/schema.prisma` + `0_init`, `docs/modules/modmail.mdx`, README.

**verified**: typecheck clean; modmail service tests 3/3 (`relayBody`/`isStaffNote`); biome clean; baseline
includes the two new tables. Known limit (documented): DMs route to shard 0, so multi-shard deploys only
match shard-0 mutual guilds.

### 2026-06-07 (advanced automod: custom rule engine)

**changed**: Extended `automod` (backlog item 12) with a custom rule engine on top of the built-in filters.
New pure `rules.ts`: `compileRule(trigger, pattern)` validates + compiles keyword (whole-word) / wildcard
(`*` -> `.*`) / regex (case-insensitive, capped at 200 chars, bad regex returns null), `matchRule` /
`firstMatchingRule` / `validateRuleInput`. New `AutomodRule` model (per-guild named rules, trigger,
pattern, per-rule action). Service gained cached `getRules/addRule/removeRule/toggleRule`, `enforce` took
an `actionOverride`, and `isExempt` is now exported. New `/automod rule add|remove|toggle|list` group;
rules evaluate after the built-in filters in the messageCreate guard, respecting the same exempt list.
Full YAGPDB condition matrix (channel/role/account-age conditions beyond the global exempt list) remains
future work.

**files touched**: `src/modules/automod/{index,service,rules,rules.test}.ts`, `prisma/schema.prisma` +
`0_init`, `docs/modules/automod.mdx`.

**verified**: typecheck clean; automod rules tests 7/7 (keyword/wildcard/regex/validation) + existing
checks tests; biome clean; baseline includes the `AutomodRule` table.

### 2026-06-07 (starboard depth)

**changed**: Extended `starboard` (backlog item 11, additive pass on the single-config model). New pure
`decideStarboard(stars, threshold, removeThreshold, hasPost) -> post|keep|remove|none` adds a hysteresis
floor so posts at the threshold do not flicker; `earnsReward(stars, rewardStars)` gates a star-reward role.
Events reworked around the decision result and grant the reward role on `post`. New subcommands
`/starboard removethreshold <count>` and `/starboard reward <stars> [role]`; richer status. New
`StarboardConfig` columns: `removeThreshold`, `rewardRoleId`, `rewardStars`. Multiple starboards / per-board
overrides deferred (need a new per-board table) and noted in the module doc.

**files touched**: `src/modules/starboard/{index,events,service,decide.test}.ts`, `prisma/schema.prisma` +
`0_init`, `docs/modules/starboard.mdx`.

**verified**: typecheck clean; starboard decide tests 5/5 + existing service tests; biome clean; baseline
includes the new columns.

### 2026-06-07 (verification depth)

**changed**: Extended `verification` (backlog item 10) with a captcha and auto-kick. New `captcha.ts` (pure
`makeCaptcha` -> sum question + four shuffled answer buttons, correctness encoded in the button id). New
`events.ts`: a `guildMemberAdd` listener schedules a durable `verify_kick` task when `kickAfterMin` is set;
the handler kicks only if the member still lacks the verified role (self-cancelling, restart-safe, no task
id tracking). New subcommands `/verification captcha <enabled>` and `/verification autokick <minutes>` (0 =
off); richer `status`. New `VerificationConfig` columns: `captcha`, `kickAfterMin`. Component flow: captcha
panel on `verify:go` when enabled, `verify:cap:ok|no:<i>` grants/retries.

**files touched**: `src/modules/verification/{index,events,captcha,captcha.test,service}.ts`,
`prisma/schema.prisma` + `0_init`, `docs/modules/verification.mdx`.

**verified**: typecheck clean; captcha test (200-iteration invariants) green; biome clean (fixed an
optional-chain warning); baseline includes the new columns.

### 2026-06-07 (anti-raid / panic mode)

**changed**: Extended `security` (backlog item 9) with anti-raid. New `raid.ts`: a second `guildMemberAdd`
handler tracks an in-memory per-guild join window (pure `recordJoin`); when `raidThreshold` joins land in
`raidWindowSec` it calls `triggerLockdown` (raise verification to Very High + alert + schedule a durable
`raid_unlock` task that restores the prior level after `raidLockMinutes`). New `/security antiraid
<enabled> [threshold] [window] [minutes]` and `/security panic <on|off>` (manual lockdown, no auto-lift).
Lock state + restore level in an in-memory Map (guild single-shard owned); scheduled payload carries
`{guildId, restoreLevel}` for restart safety. New `AntinukeConfig` columns: `antiRaid`, `raidThreshold`,
`raidWindowSec`, `raidLockMinutes`. Gated by the existing `security` tenant flag (already in `/setup` +
`/help` + emoji, so no extra wiring).

**files touched**: `src/modules/security/{index,events,raid,raid.test}.ts`, `prisma/schema.prisma` +
`0_init`, `docs/modules/security.mdx`.

**verified**: typecheck clean; security raid tests 2/2 (`recordJoin` window logic) + existing service tests;
biome clean; baseline includes the new columns.

### 2026-06-07 (server stats channels)

**changed**: New `serverstats` module (backlog item 8): locked voice channels that show live counts.
`/serverstats add|remove|list` (Manage Channels). New `StatsChannel` model (`channelId` id, `type`,
`template`). `add` creates a Connect-denied voice channel and registers it; a new `poll-stats-channels`
manager cron (every 10 min, REST-only like feeds) groups rows by guild, fetches
`GET /guilds/{id}?with_counts=true` plus channel/role lists once, and PATCHes each name via pure
`applyTemplate`. Types: members, online, boosts, channels, roles (no bots - REST cannot separate them
without privileged member listing). Wired into the `serverstats` emoji and `/help` Server Management; no
`/setup` flag (opt-in cosmetic, no hot path).

**files touched**: `src/modules/serverstats/{index,service,service.test}.ts`, `src/jobs/builtins.ts`,
`prisma/schema.prisma` + `0_init`, `src/ui/emojis.ts`, `src/modules/core/categories.ts`,
`docs/modules/serverstats.mdx`, `README.md`.

**verified**: typecheck clean; serverstats tests 4/4 (`applyTemplate`/`isStatType`/`defaultTemplate`);
categories test green; biome clean; baseline includes `StatsChannel`.

### 2026-06-07 (role menus / dropdown self-roles)

**changed**: Extended `reactionroles` (backlog item 7) with a string-select variant instead of a new module
(reuses the `ReactionRolePanel` model). New `/reactionrole postmenu <panel>` posts the panel as a `@/ui`
`stringSelect` (min 0; max 1 single / all multiple); new `rrs:<panelId>` ComponentHandler reconciles the
submission via pure `reconcileRoles(panelRoleIds, currentRoleIds, selectedIds)`, which only adds/removes
roles inside the panel set and ignores anything else the member holds or selects. No schema or wiring change
(reactionrole already in `/help` + emoji).

**files touched**: `src/modules/reactionroles/{index,service,service.test}.ts`,
`docs/modules/reactionroles.mdx`.

**verified**: typecheck clean; reactionroles tests 5/5 (added 3 `reconcileRoles` cases); biome clean.

### 2026-06-07 (bulk role)

**changed**: New `bulkrole` module (backlog item 6): `/role all add|remove <role> [who]` applies a role
across every matching member. No model. `applyRoleToAll` fetches members once, filters via pure
`matchesFilter` (everyone/humans/bots), skips members already in the target state, and paces writes (750ms
every 5) to respect the role rate limit, returning a `{changed,skipped,failed,total}` summary. Guards managed
roles, `@everyone`, and hierarchy; relies on safeAck auto-defer. Wired into the `role` emoji and `/help`
Server Management (no `/setup` gate - it is a one-shot action).

**files touched**: `src/modules/bulkrole/{index,service,service.test}.ts`, `src/ui/emojis.ts`,
`src/modules/core/categories.ts`, `docs/modules/bulkrole.mdx`, `README.md`.

**verified**: typecheck clean; bulkrole tests 3/3 (`matchesFilter`); categories test green; biome clean.

### 2026-06-07 (voice roles)

**changed**: New `voicerole` module (backlog item 5): grant a role while connected to voice, remove on
disconnect. `/voicerole set|disable|status` (Manage Roles). New `VoiceRoleConfig` model (one row per guild).
The voiceStateUpdate listener uses pure `voiceRoleAction` to map the state diff to add/remove/none, gated by
the `voicerole` tenant flag, and guards role hierarchy + managed roles in both the command and the listener.
Config cache stores `null` for known-empty guilds. Wired into `/setup`, the `voicerole` emoji, and the
`/help` Server Management category.

**files touched**: `src/modules/voicerole/{index,events,service,service.test}.ts`, `prisma/schema.prisma` +
`0_init`, `src/ui/emojis.ts`, `src/modules/core/categories.ts`, `src/modules/setup/index.ts`,
`docs/modules/voicerole.mdx`, `README.md`.

**verified**: typecheck clean; voicerole tests 2/2 (`voiceRoleAction`); categories + setup tests green;
biome clean.

### 2026-06-07 (reputation)

**changed**: New `reputation` module (backlog item 4): `/rep give|view|leaderboard`. New `RepUser` model
(composite id; `points` received, `lastGiven` giver cooldown). `giveRep` enforces no-self + 24h cooldown,
then increments the receiver and syncs a Redis sorted set `lb:rep:<guild>` (DB fallback), mirroring
economy/leveling. Wired into the `reputation` emoji and the `/help` Economy & Levels category; not added to
the `/setup` kill-switch (low-risk social command, no hot path).

**files touched**: `src/modules/reputation/{index,service,service.test}.ts`, `prisma/schema.prisma` +
`0_init`, `src/ui/emojis.ts`, `src/modules/core/categories.ts`, `docs/modules/reputation.mdx`, `README.md`.

**verified**: typecheck clean; reputation tests 3/3 (`cooldownRemaining`); categories test green; biome clean.

### 2026-06-07 (sticky messages)

**changed**: New `sticky` module (backlog item 3): keep a message pinned to the bottom of a channel.
`/sticky set|remove|list` (Manage Server). New `StickyMessage` model (`channelId` id, `lastMessageId`). The
messageCreate handler is cached per guild and gated by the `sticky` tenant flag; a 3s per-channel debounce
deletes the old repost and sends a fresh one (`buildStickyContent` adds 📌 and clamps to 2000), writing the
new id back to DB + the shared cache row so the next delete is correct. Wired into `/setup`, the `sticky`
emoji, and the `/help` Server Management category.

**files touched**: `src/modules/sticky/{index,events,service,service.test}.ts`,
`prisma/schema.prisma` + `0_init`, `src/ui/emojis.ts`, `src/modules/core/categories.ts`,
`src/modules/setup/index.ts`, `docs/modules/sticky.mdx`, `README.md`.

**verified**: typecheck clean; sticky tests 2/2 (`buildStickyContent`); biome clean; baseline migration
includes `StickyMessage`.

### 2026-06-07 (highlights)

**changed**: New `highlight` module (backlog item 2): keyword highlights that DM a subscriber when one of
their words is said. `/highlight add|remove|list|clear` (personal, no Manage perms). New `Highlight` model
(unique guild+user+word). messageCreate scan is cached per guild and gated by the `highlights` tenant flag;
`resolveHits` does a whole-word case-insensitive match, excludes the author / recently-active / on-cooldown
subscribers, then the handler fetches each member, checks `ViewChannel`, and DMs a jump link + snippet.
Wired into `/setup` FEATURES (`highlights`), the `highlight` emoji, and the `/help` Utility category. Baseline
migration regenerated (note: Prisma now wants `--to-schema <path>`, the old `--to-schema-datamodel` was
removed).

**files touched**: `src/modules/highlight/{index,events,service,service.test}.ts`,
`prisma/schema.prisma` + `prisma/migrations/0_init/migration.sql`, `src/ui/emojis.ts`,
`src/modules/core/categories.ts`, `src/modules/setup/index.ts`, `docs/modules/highlight.mdx`, `README.md`.

**verified**: typecheck clean; highlight tests 4/4 (wordMatches + resolveHits), core/loader tests still
green; biome clean on touched files.

### 2026-06-07 (feeds: reddit + rss)

**changed**: Extended the `feeds` module with two keyless sources, working the backlog top-down (item 1 of
`docs/superpowers/plans/2026-06-07-remaining-features-backlog.md`). `/feeds reddit <subreddit>` polls
`/r/<sub>/new/.rss`; `/feeds rss <url>` polls any RSS 2.0 or Atom feed. Added a single `parseGenericFeed`
(handles `<entry>` and `<item>`, CDATA, XML entities) plus `redditPreview`/`rssPreview` and a `User-Agent`
header (Reddit rejects the default UA). No schema change: `FeedSubscription.type`/`sourceId` are free
strings, so `FeedType` just gained `reddit|rss`. Seeds `lastItemId` on add so subscribing never replays
history.

**files touched**: `src/modules/feeds/{index,service,service.test}.ts`, `docs/modules/feeds.mdx`.

**verified**: typecheck clean; feeds tests 6/6 (3 new `parseGenericFeed` cases); biome clean on the module
(repo-wide `biome check .` noise is only from the gitignored `Archive/` JS, not in CI scope).

### 2026-06-05

**changed**: Project created. Phase 1 audit of 15 archived bots completed. Architecture designed and
approved (single modular TS package; Postgres+Prisma+Redis; native ShardingManager with hybrid switch;
slash-only + Components V2; no dashboard; no premium; undercover mode). Private repo created. **Plan 1a
(Foundation) implemented and merged to `main`**: config loader, Pino logger, Prisma core schema +
`getPrisma`, Redis/Keyv cache, Components V2 kit, i18n, sharding IPC abstraction. 14/14 tests pass;
typecheck + Biome clean. top.gg + multi-node Lavalink folded into the spec; 10 Lavalink nodes stored in
local `.env`. Documentary docs system set up.

**files touched**: `package.json`, `tsconfig.json`, `tsup.config.ts`, `biome.json`, `.env.example`,
`prisma/schema.prisma`, `src/config.ts`, `src/services/{logger,database,cache}.ts`,
`src/utils/components.ts`, `src/i18n/index.ts` + `locales/en/common.json`, `src/cluster/ipc.ts`,
`src/types/global.d.ts` (+ matching `*.test.ts`).

**docs updated**: created `docs/overview.mdx`, `stack.mdx`, `style.mdx`, `architecture/index.mdx`,
`modules/{config,services,components,i18n,ipc}.mdx`, `infrastructure/{env,deployment}.mdx`, this log;
specs/plans under `docs/superpowers/`.

**decisions made**: Postgres over Turso (write-heavy at 1M DAU). Slash-only. Components V2 only. No
premium (all free). Undercover mode is a hard invariant (no AI-provider references). Stats autopost from
the manager process, not per-shard. Bot name = "Rostra".

**next**: Plan 1b (Runtime) - DONE (see next entry). Then feature modules (moderation first).

### 2026-06-05 (later - Plan 1b Runtime)

**changed**: Implemented the full runtime layer. Module contract (`SlashCommand`/`ComponentHandler`/
`RegisteredEvent`/`JobDefinition`/`BotModule`), `BotClient` with auto-loaders, `defineEvent` typed
factory, command pipeline (permissions → cooldown → error boundary), interaction router, BullMQ job
queue (in-memory fallback), top.gg service + Fastify health/metrics + `/votes/topgg` webhook + stats
autoposter, `cluster.ts` (ShardingManager) + `bot.ts` shard entry, graceful shutdown, deploy-commands,
sample `core` module (`/ping`, `/help`), Dockerfile + docker-compose + README + CI. **Live login verified**
(connected as Felisha#3245). 33/33 tests pass, typecheck + lint clean, embed guard clean.

**decisions made**: Run via **tsx in dev AND prod** (dropped tsup) - bundling collapses `import.meta.url`
which breaks filesystem module discovery; running TS directly keeps drop-in modules working. Renamed env
`SHARD_COUNT`→`TOTAL_SHARDS` to avoid colliding with discord.js's reserved sharding env var. top.gg stats
posted from the manager process (aggregate across shards). Removed unused `tsup`/`topgg-autoposter` deps;
moved `tsx` to runtime deps.

**files touched**: `src/types/module.ts`, `src/client/{BotClient,defineEvent}.ts`,
`src/client/loaders/*`, `src/pipeline/*`, `src/interactions/router.ts`, `src/jobs/queue.ts`,
`src/services/topgg.ts`, `src/web/{server,autopost}.ts`, `src/lifecycle/shutdown.ts`,
`src/modules/core/index.ts`, `src/scripts/deploy-commands.ts`, `src/{cluster,bot}.ts`, `Dockerfile`,
`docker-compose.yml`, `README.md`, `.github/workflows/ci.yml`, `package.json` (+ tests).

**next**: First feature module - moderation (DONE, see next entry).

### 2026-06-05 (later - moderation module)

**changed**: Built the first feature module. `/mod` with 15 subcommands (ban, unban, kick, timeout,
untimeout, warn, warnings, removewarn, cases, note, purge, slowmode, lock, unlock, nick). New Prisma
models `ModerationCase` (warnings/notes share it via `type`) + `GuildCaseCounter` (per-guild sequential
case numbers via a transactional counter). Service ports TitanBot's hierarchy/bot-hierarchy guards and
`kickable`/`moderatable` checks into TS, persisting cases relationally (vs TitanBot's KV). Duration
parser (`10m`/`1h30m`/`2d`) with tests. All replies Components V2; all strings `moderation:*` i18n keys;
per-subcommand permission checks. 37/37 tests pass, typecheck + lint + embed guard clean; loader shows
`help, mod, ping`.

**files touched**: `src/modules/moderation/{index,service,duration}.ts` (+ `duration.test.ts`),
`prisma/schema.prisma`, `docs/modules/moderation.mdx`.

**decisions made**: Unify warnings/notes/infractions in one `ModerationCase` table keyed by `type`.
Per-guild case numbers from a `GuildCaseCounter` row incremented in the same transaction as the insert.
Soft-delete cases (`active=false`), never hard-delete. `/mod` uses one top-level command + subcommands
(stays well under the 100-command cap) with `ModerateMembers` default visibility + per-subcommand perm
checks.

**next**: see utility entry below, then security/antinuke.

### 2026-06-05 (later - utility module + V2 kit extension)

**changed**: Added the **utility** module: `/util avatar|banner|userinfo|serverinfo` (read-only, no DB).
Extended the Components V2 kit (`src/utils/components.ts`) with `gallery()` (media gallery from image
URLs) and `section()` (text block with thumbnail accessory), widening the container/top-level unions to
accept them. 39/39 tests pass; typecheck + lint + embed guard clean; loader shows `help, mod, ping, util`.

**files touched**: `src/modules/utility/index.ts`, `src/utils/components.ts` (+ `components.test.ts`),
`docs/modules/utility.mdx`.

**decisions made**: `banner` force-fetches the user (banners aren't on the cached partial). Timestamps use
Discord `<t:unix:F|R>` markup. The `gallery`/`section` helpers are the reusable path for any module that
needs images - modules must not hand-roll V2 builders.

**next**: security/antinuke (DONE - see next entry).

### 2026-06-05 (later - security/antinuke module)

**changed**: Built the antinuke module. `/security` (Admin-only) config command: enable/disable/status,
punishment (ban|kick|strip), logchannel, notify, per-module toggle, and whitelist/extraowner add/remove
groups. 9 detection events (channelCreate/Delete, roleCreate/Delete, guildBanAdd, guildMemberRemove,
guildMemberAdd-bots, webhooksUpdate, guildUpdate) attribute the actor via audit log (`resolveAuditExecutor`,
ported from i-karen with retry + TTL) and `punish()` the untrusted executor (hierarchy-checked, with
strip-roles fallback) + log + owner DM. New `AntinukeConfig` model with `String[]` whitelist/extraOwners.
Config cached per-process. 41/41 tests pass; typecheck + lint + embed guard clean; loader registers
`help, mod, ping, security, util`.

**files touched**: `src/modules/security/{index,events,audit,service}.ts` (+ `service.test.ts`),
`prisma/schema.prisma`, `docs/modules/security.mdx`.

**decisions made**: Immediate-punish-on-unauthorized-action (core antinuke) rather than i-karen's
threshold/panic system (deferred). Single trusted whitelist + extraOwners (vs per-action whitelist).
v1 does not auto-revert deleted resources. Antinuke config in its own table, not the generic
`GuildModuleConfig`, for typed per-field access in hot event paths.

**next**: automod (DONE - see next entry).

### 2026-06-05 (later - automod module)

**changed**: Built automod. `/automod` (Manage Server) config: enable/disable/status, action
(delete|warn|timeout), per-filter toggle, limits, logchannel, exempt role/channel groups, custom-word
groups. messageCreate guard runs `checkMessage` (invite, link w/ allow-list, mass-mention, caps,
profanity via `bad-words`, spam sliding-window) and `enforce` (delete + optional timeout + log). New
`AutomodConfig` model; config + per-guild `bad-words` Filter cached. Pure detectors in `checks.ts` with
tests. 44/44 tests pass; typecheck + lint + embed guard clean; loader registers `automod, help, mod,
ping, security, util`.

**files touched**: `src/modules/automod/{index,service,checks}.ts` (+ `checks.test.ts`),
`prisma/schema.prisma`, `docs/modules/automod.mdx`.

**decisions made**: Used the `bad-words` dependency (per project requirement) as the profanity engine,
wrapped with a per-guild cached Filter + custom words. Spam tracking is in-memory per-process (correct
since a guild's traffic lands on one shard). Fixed check order; first hit wins; exempt roles/channels
short-circuit.

**next**: logging (DONE - see next entry).

### 2026-06-05 (later - logging module)

**changed**: Built audit logging. `/logging` (Manage Server): channel, toggle <type>, status, ignore
add/remove group. 10 event handlers (message delete/edit, member join/leave/ban/unban, role create/delete,
channel create/delete) → `sendLog` posts a Components V2 entry to the configured channel when the type is
enabled. New `LoggingConfig` model; cached. `truncate` helper tested. 46/46 tests pass; typecheck + lint
+ embed guard clean; loader registers `automod, help, logging, mod, ping, security, util`.

**files touched**: `src/modules/logging/{index,events,service}.ts` (+ `service.test.ts`),
`prisma/schema.prisma`, `docs/modules/logging.mdx`.

**decisions made**: `sendLog` takes a builder thunk (container only built when the event is enabled).
Message events skip bots + ignored channels; partials show "-" for missing content.

**next**: welcome/autorole (DONE - see next entry).

### 2026-06-05 (later - welcome/autorole module)

**changed**: `/welcome` (Manage Server): channel/message/toggle for welcome|goodbye|dm, test preview,
status, autorole add/remove group. guildMemberAdd (autorole + welcome post + welcome DM) and
guildMemberRemove (goodbye) handlers. `WelcomeConfig` model; `formatMessage` placeholder templating
({user}/{username}/{server}/{membercount}) with tests. 48/48 tests pass; clean; loader has 8 commands.

**files touched**: `src/modules/welcome/{index,events,service}.ts` (+ test), `prisma/schema.prisma`,
`docs/modules/welcome.mdx`.

**next**: tickets (DONE - see next entry).

### 2026-06-05 (later - tickets module)

**changed**: First component-interaction module. `/ticket` (Manage Server): enable/disable/status, panel,
category, logchannel, close, claim, add, supportrole add/remove group. Button handlers (`ComponentHandler`
prefix `ticket`): `ticket:open` (creates private channel w/ overwrites + control buttons), `ticket:claim`,
`ticket:close` (logs + deletes after 5s). Models `TicketConfig`, `Ticket`, `GuildTicketCounter`
(sequential numbers). 48/48 tests pass; clean; loader has 9 commands + component prefix `ticket`.
**The interaction router → ComponentHandler path is now exercised end-to-end.**

**files touched**: `src/modules/tickets/{index,service}.ts`, `prisma/schema.prisma`,
`docs/modules/tickets.mdx`.

**decisions made**: One open ticket per user/guild. Buttons via customId `prefix:action`. Close deletes
channel after a delay; no HTML transcript yet (future). Tickets is the reference for component modules.

**next**: economy (DONE - see next entry).

### 2026-06-05 (later - economy module)

**changed**: `/economy` with 11 subcommands (balance, daily, work, beg, crime, rob, pay, deposit,
withdraw, gamble, leaderboard). `EconomyUser` model (composite id). `updateBalance` clamps + mirrors net
worth to a Redis sorted set `lb:eco:<guild>`; `leaderboard` reads Redis with DB fallback. Cooldowns via
timestamp columns; pure `formatCoins`/`cooldownRemaining` tested. 51/51 tests; clean; 11 commands loaded.
**First module to use `getRedis()` sorted sets.**

**files touched**: `src/modules/economy/{index,service}.ts` (+ test), `prisma/schema.prisma`,
`docs/modules/economy.mdx`.

**decisions made**: Int balances clamped to 1e9 (avoid 32-bit overflow). Redis leaderboard with DB
fallback. Daily streak continues if claimed within 48h.

**next**: leveling (DONE - see next entry).

### 2026-06-05 (later - leveling module)

**changed**: `/level` (rank, leaderboard, enable, disable, announcechannel, set, reward add/remove/list).
XP on messageCreate w/ per-user in-memory cooldown; `LevelConfig`/`LevelUser`/`LevelReward` models; pure
curve `totalXpForLevel`/`levelFromXp`/`levelProgress` tested; Redis sorted set `lb:level:<guild>` + DB
fallback; role rewards granted on level-up (back-fills ≤ level). 54/54 tests; clean; 12 commands loaded.

**files touched**: `src/modules/leveling/{index,events,service}.ts` (+ test), `prisma/schema.prisma`,
`docs/modules/leveling.mdx`.

**next**: giveaways (DONE - see next entry).

### 2026-06-05 (later - giveaways module)

**changed**: `/giveaway start|end|reroll|list` + `giveaway:enter:<id>` button. `Giveaway` model;
`pickWinners` (Fisher–Yates, tested). **Auto-end via per-shard `setTimeout`, not BullMQ** (giveaways are
guild-bound; the owning shard schedules, and reschedules active ones on `ready` for guilds it can see) -
avoids cross-shard worker mis-routing. 56/56 tests; clean; 13 commands, components `giveaway, ticket`.

**files touched**: `src/modules/giveaways/{index,service}.ts` (+ test), `prisma/schema.prisma`,
`docs/modules/giveaways.mdx`.

**decisions made**: Chose per-shard timers over BullMQ for guild-bound scheduling. Entries as `String[]`
on the row. `setTimeout` capped at ~24.8d, re-arms on fire/restart.

**next**: joinToCreate (DONE - see next entry).

### 2026-06-05 (later - joinToCreate module)

**changed**: `/voicehub` (set/nametemplate/limit/disable/status). voiceStateUpdate handler creates a temp
voice channel when a member joins the hub (owner gets channel-management perms, moved in) and deletes
tracked temp channels when empty. Models `JoinToCreateConfig`, `J2CChannel`. `formatName` tested. 58/58
tests; clean; 13 commands.

**files touched**: `src/modules/joinToCreate/{index,events,service}.ts` (+ test), `prisma/schema.prisma`,
`docs/modules/joinToCreate.mdx`.

**next**: reactionroles + birthday (BOTH DONE - see entries below).

### 2026-06-05 (later - reactionroles + birthday modules)

**reactionroles**: `/reactionrole create|addrole|removerole|post|list|delete` (Manage Roles) + button
handler `rr:<panelId>:<roleId>` toggling roles (single/multiple mode). `ReactionRolePanel` (roles in JSON,
`parseRoles` validates + tested). Up to 25 buttons/panel. Components now `giveaway, rr, ticket`.

**birthday**: `/birthday set|remove|view|next` + (Manage Server) `channel|role|message`. Per-shard daily
09:00 UTC scheduler announces today's birthdays for owned guilds + grants a temp role (~23h). Models
`BirthdayConfig`, `Birthday`. Pure `isValidDate`/`msUntilNextRun` tested.

**totals**: 62/62 tests; clean; **15 top-level commands**, 3 component prefixes. Per-shard scheduling
reaffirmed as the pattern for guild-bound time tasks (BullMQ reserved for non-guild work).

**files touched**: `src/modules/reactionroles/{index,service}.ts`, `src/modules/birthday/{index,events,
service}.ts` (+ tests), `prisma/schema.prisma`, `docs/modules/{reactionroles,birthday}.mdx`.

**next**: extras (DONE - see next entry).

### 2026-06-05 (later - extras module)

**changed**: `extras` module exporting THREE commands: `/afk [reason]`, `/snipe`, `/autoresponder
add|remove|list`. messageCreate handles AFK clear/mention + autoresponder; messageDelete stores snipe;
ready loads AFK. Models `AfkStatus` (in-memory authoritative + DB-backed), `AutoResponder` (cached). Snipe
is in-memory only. Pure `matchResponder` tested. 64/64 tests; clean; **18 top-level commands**.

**files touched**: `src/modules/extras/{index,events,service}.ts` (+ test), `prisma/schema.prisma`,
`docs/modules/extras.mdx`.

**decisions made**: One module can export multiple top-level commands. Conversational replies (afk/AR) use
plain `content` (allowed; embed-ban is about `EmbedBuilder`, not plain text). AFK kept in-memory for
fast per-message checks, DB for restart recovery.

**Remaining (4 specialized modules, larger - good for a fresh context):**
- **trivia** - self-contained quiz game; Open Trivia DB API (`node-fetch`), question/answer buttons,
  per-user scores + leaderboard.
- **games** - button mini-games (tictactoe, connect4, rps, wordle…); mostly stateless component flows.
- **ai** - chat via neutral `AI_*` env (provider hidden); **undercover-critical**: persona "Rostra" only,
  no provider names anywhere. Needs an AI client wrapper around `AI_BASE_URL`/`AI_API_KEY` (OpenAI-compatible).
- **music** - Lavalink (10 nodes already in `.env` as `LAVALINK_NODES`); pick `lavalink-client`/shoukaku;
  `/music play|queue|skip|…`; needs a running node to smoke-test (user has them).

### 2026-06-05 (later - ai module)

**changed**: `/ask <question>` assistant. `ask()` POSTs to `${AI_BASE_URL}/chat/completions`
(OpenAI-compatible) with a generic persona + the question; `sanitizeReply` strips leak phrases.
**Undercover verified**: repo-wide `grep -riE "openai|anthropic|claude|groq|gpt|gemini|llama|mistral" src`
returns clean - no provider name anywhere (persona is generic, no denylist of names in source). Deferred
reply + 5s cooldown; graceful "unavailable" when `AI_*` unset. 67/67 tests; clean; **19 commands**.

**files touched**: `src/modules/ai/{index,service}.ts` (+ test), `docs/modules/ai.mdx`.

**decisions made**: No provider SDK (plain `fetch` to OpenAI-compatible endpoint) to avoid any package/name
leak. Persona instructs generically ("never name the technology powering you") rather than listing
vendors (listing would itself violate undercover). `sanitizeReply` handles generic leak phrases only.

**Remaining (3 modules): trivia, games, music** - larger/specialized; best with fresh context. music needs
a running Lavalink node to smoke-test (nodes are in `.env`).

### 2026-06-05 (later - safeAck core utility)

**changed**: Added `src/utils/safeAck.ts` (`withSafeAck`) - auto-defers any interaction whose handler
hasn't replied within ~2.3s (margin under Discord's 3000ms deadline), showing the native "thinking…" state
so slow handlers never fail with "Unknown interaction". Wired centrally into `pipeline/runCommand.ts`
(commands) and `interactions/router.ts` (components/modals). Checks `replied||deferred` first so
self-deferring handlers (/ask, ticket close) aren't double-acked. Optional `deferEphemeral` added to
`SlashCommand`/`ComponentHandler`. 70/70 tests (3 new); typecheck + lint + undercover clean; 19 commands.

**files touched**: `src/utils/safeAck.ts` (+ test), `src/pipeline/runCommand.ts`,
`src/interactions/router.ts`, `src/types/module.ts`.

### 2026-06-05 (later - safeAck heartbeat + V2 follow-up fallback)

**changed**: Added an opt-in `heartbeat` to `withSafeAck` - for long-running handlers it edits the
deferred reply every ~8s ("⏳ Rostra is still working on it… (Ns)"), and a graceful note as the ~15-min
interaction token nears expiry; stops the instant the handler finishes. Heartbeat uses Components V2 to
stay flag-consistent with the final reply. Added `heartbeat?` to `SlashCommand`/`ComponentHandler`
(passed through pipeline + router); enabled on `/ask`. **De-risked the deferred→V2 path**: the components
kit's `send()` now falls back to a fresh `followUp` if an `editReply` with the IsComponentsV2 flag is ever
rejected (Discord can refuse toggling that flag on edit), so auto-deferred commands never fail to render.
72/72 tests (2 new heartbeat tests); typecheck + lint + embed + undercover clean; 19 commands.

**files touched**: `src/utils/safeAck.ts` (+ test), `src/utils/components.ts`, `src/types/module.ts`,
`src/pipeline/runCommand.ts`, `src/interactions/router.ts`, `src/modules/ai/index.ts`.

**verify-on-live note**: confirm `deferReply()` → `editReply({ flags: IsComponentsV2, components })` renders
in one message on a real interaction; if Discord rejects it, the followUp fallback fires (one extra message)
- functional either way, worth a quick eyeball during smoke-testing.

### 2026-06-05 (later - trivia module)

**changed**: `/trivia play|score|leaderboard` + answer buttons (`trivia:answer:<token>:<i>`, ephemeral).
Fetches from Open Trivia DB (`encode=url3986` → `decodeURIComponent`), shuffles options
(`buildOptions`, tested), tracks round state in an in-memory token map (60s TTL, one answer/user),
records `TriviaScore` + Redis `lb:trivia:<guild>`. 74/74 tests; clean; **20 commands**, components
giveaway/rr/ticket/trivia.

**files touched**: `src/modules/trivia/{index,service}.ts` (+ test), `prisma/schema.prisma`,
`docs/modules/trivia.mdx`.

**next**: games (DONE - see next entry).

### 2026-06-05 (later - games module)

**changed**: `/game tictactoe <opponent>` (live 3×3 button grid, turn-validated, `interaction.update`
re-renders) + `/game rps` (vs bot, ephemeral). Pure `checkTicTacToe`/`rpsOutcome` tested; tic-tac-toe
state in-memory (token, 10-min TTL); no DB. 78/78 tests; clean; **21 commands**, components
game/giveaway/rr/ticket/trivia.

**files touched**: `src/modules/games/{index,service}.ts` (+ test), `docs/modules/games.mdx`.

**next (LAST module): music** - DONE (see below).

### 2026-06-05 (later - music module = ALL MODULES COMPLETE 🎉)

**changed**: `/music play|skip|stop|pause|resume|queue|nowplaying|volume|loop` via **lavalink-client v2**
(Lavalink v4). `getManager` builds a multi-node manager from `config.lavalink.nodes`; `initMusic` (on
`ready`) wires node + track events and the untyped `"raw"` gateway listener. Lazy/guarded - `/music` says
"unavailable" with no nodes. 78/78 tests; typecheck + lint + embed + undercover clean; **22 commands**.

**files touched**: `src/modules/music/{index,service,events}.ts`, `package.json`, `docs/modules/music.mdx`.

**verify-on-live note**: music needs a real Lavalink node to confirm playback (typechecks + loads; 10
public nodes are in `.env`). Everything else is unit-tested/loaded.

## ✅ PROJECT COMPLETE - full AIO

All 15 archived bots' features are ported into Rostra. **17 feature modules** (moderation, security,
automod, logging, welcome, tickets, economy, leveling, giveaways, joinToCreate, reactionroles, birthday,
extras, utility, trivia, games, music, ai) + the full core platform (config, services, Components-V2 kit,
i18n, sharding IPC, BotClient + loaders, command pipeline + safeAck, interaction router, jobs, Fastify
health + top.gg, cluster/shard entries, graceful shutdown). **22 top-level commands**, 5 component
prefixes. 78/78 tests; tsc + Biome + embed-guard + undercover all clean; live login verified.

**Added:** `MIGRATION.md` (decommission guide).

### 2026-06-05 (later - UI library + ops items)

**UI library** (`src/ui/`, import `@/ui`): centralized Components-V2 toolkit - primitives (`button`,
`linkButton`, `actionRow`, `buttonGrid`, `stringSelect`, `modal`, `textInput`) + patterns (`confirmRow`,
`toggleButton`/`settingsPanel`, `paginatorRow`); re-exports the layout kit. customId convention
`module:action:arg` so module ComponentHandlers wire behavior. 83/83 tests; `docs/modules/ui.mdx`.

**4 ops items - all done:**
1. ✅ **Baseline DB migration** generated via `migrate diff` → `prisma/migrations/0_init/migration.sql`
   (29 tables) + `migration_lock.toml`. `npm run prisma:migrate` now = `prisma migrate deploy`.
2. ✅ **Slash commands registered** globally with Discord - **22 commands** (verified via REST.put). Fixed
   `deploy:commands` npm script to load env (`--env-file-if-exists`).
3. ✅ **Lavalink verified** - added `src/scripts/lavalink-check.ts` (`npm run lavalink:check`); **7/10 nodes
   connect** (real WS auth). 3 fail node-side (expired cert / 401 / timeout) - failover covers them. Music
   connection layer confirmed working; in-VC playback still needs a human to fully exercise.
4. ✅ **Archive/ deleted** (60M, 13 source-bot folders) now that all features are ported + verified.

**Manual-only leftover:** rename the Discord application "Felisha" → "Rostra" in the Developer Portal
(browser action; can't be automated). Bot is otherwise production-ready: `npm run prisma:migrate` on the
live DB, then `npm start`.

### 2026-06-05 (later - niche features + @/ui invariant + rewiring)

**changed**: Added the 3 leftover niche modules - **tags** (`/tag get|add|remove|list`; `Tag` model),
**vanityroles** (`/vanityrole set|disable|status` + `presenceUpdate` keyword→role; `VanityRoleConfig`;
**added `GuildPresences` intent**), **feedback** (`/feedback send|setup`; `FeedbackConfig`). New
**invariant**: all UI must come from `@/ui` - never hand-roll discord.js component builders; if `@/ui`
lacks something, add it there first (in CLAUDE.md CIT-INVARIANTS + `docs/style.mdx`). Rewired
**tickets / giveaways / reactionroles** to build buttons via `@/ui` (`button`/`actionRow`/`buttonGrid`).
Baseline migration regenerated → **32 tables**. 85/85 tests; tsc + Biome + embed + undercover clean;
**25 top-level commands**.

**files touched**: `src/modules/{tags,vanityroles,feedback}/*`, `src/modules/{tickets,giveaways,
reactionroles}/index.ts`, `src/client/BotClient.ts`, `prisma/schema.prisma` (+ `migrations/0_init`),
`docs/style.mdx`, `docs/modules/{tags,vanityroles,feedback}.mdx`, CLAUDE.md.

**note**: `GuildPresences` is privileged - enable it in the Dev Portal for vanityroles to work.

### 2026-06-05 (later - UI rewire complete, public repo, community files, CI fix)

**changed**: Rewired **trivia + games** to `@/ui` (`button`/`actionRow`) - **no module hand-rolls
discord.js component builders anymore** (verified by grep + a new CI guard). **Fixed CI** (the failing
workflow): Node 20's test runner can't expand the `src/**/*.test.ts` glob → bumped CI to **Node 22**, added
placeholder env vars to the job (so the eager config loads), and added CI guards for embeds, undercover,
and the `@/ui` invariant. **Made the repo PUBLIC** with description + topics; enabled issues + private
vulnerability reporting. Added public community files: polished `README.md` (undercover, no support
server), `LICENSE` (MIT), `CONTRIBUTING.md`, `SECURITY.md`, `CODE_OF_CONDUCT.md`, issue + PR templates.
85/85 tests; typecheck + lint + embed + undercover + UI guards clean; 25 commands.

**files touched**: `src/modules/{trivia,games}/index.ts`, `.github/workflows/ci.yml`,
`.github/ISSUE_TEMPLATE/*`, `.github/PULL_REQUEST_TEMPLATE.md`, `README.md`, `LICENSE`, `CONTRIBUTING.md`,
`SECURITY.md`, `CODE_OF_CONDUCT.md`.

**note**: music has no buttons (text-only), so it's already invariant-compliant. CI now enforces all three
guards (embeds / undercover / no-raw-builders-in-modules) on every push & PR.

### 2026-06-05 (later - CHANGELOG, AGENTS.md, Docker publishing)

**changed**: Added `CHANGELOG.md` (Keep-a-Changelog, 0.1.0), `AGENTS.md` (committed tool-agnostic guide for
Claude Code / Gemini CLI / Codex - mirrors the invariants since `CLAUDE.md` is gitignored), and a **GHCR
Docker publish workflow** (`.github/workflows/docker.yml`) that builds + pushes `ghcr.io/astoristhebrave/
rostra` on push to `main` and version tags. README links both + a GHCR pull command.

**verified live**: CI workflow now **passes** (was failing pre-fix). Docker publish workflow **passes**
after adding `docker/setup-buildx-action` (gha cache needs the container driver).

**still manual (can't automate):** enable the **Presence** privileged intent in the Discord Developer
Portal (for `/vanityrole`); add a support-server invite to the README once one exists; the GHCR package may
need to be flipped to **public** in the repo's Packages settings so `docker pull` works without auth.

### 2026-06-06 (automation - Dependabot, release-please, badges)

**changed**: Added repo automation. (1) **Dependabot** (`.github/dependabot.yml`) - weekly `npm`
(minor/patch grouped into one PR, `build:`-prefixed) + `github-actions` (`ci:`-prefixed) updates.
(2) **Automated releases** via **release-please** (`googleapis/release-please-action@v4`, manifest mode:
`release-please-config.json` + `.release-please-manifest.json` baselined at `0.1.0`, `release-type: node`,
`include-v-in-tag`, `bump-*-pre-major` so 0.x feats bump patch). On conventional commits it opens a release
PR that bumps `package.json` + `CHANGELOG.md`; merging tags `vX.Y.Z` and cuts a GitHub release.
(3) **README badges**: CI status, latest release, GHCR image.

**Docker gotcha handled**: release-please tags via `GITHUB_TOKEN`, which does **not** cascade-trigger
`docker.yml`'s `tags: v*` push. So `docker.yml` gained a `workflow_call` trigger with a `version` input
(feeds `type=raw` version/`v`version/`latest` image tags), and `release-please.yml` has a `publish-image`
job that `uses: ./.github/workflows/docker.yml` gated on `release_created` - same run, so the versioned
image always builds. The existing push-to-`main` + `tags: v*` triggers are untouched (manual tags still
publish). Nothing broken.

**verified**: typecheck + Biome + 85/85 tests green locally. Workflow YAML/JSON validity confirmed on push
(watch the Actions tab). release-please will not open a PR until a `feat`/`fix` lands (recent commits are
`docs`/`ci`/`build`, which are hidden + non-bumping by config) - expected.

**still manual:** for the release-please **publish-image** path to push to GHCR, `GITHUB_TOKEN` `packages:
write` must be allowed (set at the job level - default repo workflow permissions suffice). Same prior
manual items stand (Presence intent; GHCR package public).

### 2026-06-06 (later - Dependabot triage, all 14 PRs resolved)

**changed**: Triaged the 14 Dependabot PRs the automation opened. Used **grouped manual commits** rather
than 14 sequential merges (each PR conflicts on `package.json`/lock or shared workflow files, so one-by-one
merges cascade-conflict). Strategy: CI runs full typecheck+lint+85-tests on every PR, so a green PR =
the bump is verified.

- **Merged green npm majors** in one commit (#6 pino 10, #13 zod 4, #8 @keyv/redis 5, #7 @types/node 25) -
  typecheck + Biome + 85/85 tests green on the combined upgrade.
- **Merged action bumps** in one commit (#2 checkout v6, #1 setup-buildx v4, #3 login v4, #5 build-push v7,
  #4 release-please v5) - validated on the next `main` push (CI, Docker publish, and Release all ran green,
  so the new action majors work end-to-end).
- **Merged typescript 6 + biome 2** (#9, #12). TS6: removed the now-deprecated tsconfig `baseUrl`; `paths`
  resolve relative to `tsconfig.json` under `moduleResolution: Bundler` (future-proof for TS7). Biome 2:
  ran `biome migrate` (config → `files.includes` + `assist.organizeImports`); the formatter major
  **reformatted 65 files** to its new defaults (one-time churn). `biome check` exits 0 with 16 non-blocking
  warnings (`useOptionalChain`×13, `noUnusedImports`×3) - left as an optional cleanup.
- **Held + closed prisma 7** (#11 client, #14 CLI). Prisma 7 is a breaking architectural change: it removes
  `url = env(...)` from the `datasource` block (P1012) and requires a `prisma.config.ts` + a `@prisma/
  adapter-pg` driver adapter passed to `PrismaClient` (changes to `schema.prisma`, `services/database.ts`,
  `config.ts`, Docker, and the migrate flow). Client + CLI must bump together (neither passes alone).
  Closed both with an explanation; tracked as a separate task. Prisma stays on v6.

**files touched**: `package.json` + `package-lock.json`, `tsconfig.json`, `biome.json`, the three
`.github/workflows/*.yml`, and the 65 biome-reformatted source files.

**note**: pre-existing `npm audit` finding (not introduced here): `undici` (high+moderate) is transitive
via `@top-gg/sdk`; `npm audit fix --force` *downgrades* top.gg (wrong). A real fix is an `overrides` pin on
undici, but undici is shared with discord.js's HTTP stack, so test before applying. Deferred.

### 2026-06-06 (later - Prisma 7 migration)

**changed**: Upgraded `@prisma/client` + `prisma` from v6 to **v7.8.0** (the upgrade Dependabot #11/#14
could not do). Prisma 7 is engine-less and moves the connection URL out of the schema, so the migration
touched four things:

- `prisma/schema.prisma`: removed `url = env("DATABASE_URL")` from the `datasource` block (the v7 P1012
  blocker). Kept the `prisma-client-js` generator: it still works under v7 (verified `prisma generate`),
  so all 22 `import type { X } from "@prisma/client"` call sites stay unchanged. No new generator/output
  path or barrel re-export needed; the single-source import pattern is preserved.
- New `prisma.config.ts` at repo root (`defineConfig` from `prisma/config`) supplies the datasource URL to
  the CLI for migrations via `env("DATABASE_URL")`, with `import "dotenv/config"` to load `.env` locally.
  Added `dotenv` as a devDependency for that.
- `src/services/database.ts`: `getPrisma()` now builds the client on the `@prisma/adapter-pg` driver
  adapter (`new PrismaPg({ connectionString: config.database.url })`). URL still comes from the validated
  `config`, never raw `process.env`. Added `@prisma/adapter-pg` (bundles `pg` + `@types/pg`).
- `Dockerfile`: copy `prisma.config.ts` before `prisma generate`; pass a build-time placeholder
  `DATABASE_URL` on the generate step (generate is offline and `env()` is strict about the var existing,
  but the value is unused); bumped base image to `node:22-alpine` (matches CI, engine-less client has no
  native-binary concern on alpine).

**verified**: `prisma generate` works both with config and offline (placeholder URL, no `.env`).
`prisma migrate status` loads `prisma.config.ts`, resolves the URL, and connects (only fails on P1001 here
because no local Postgres is running, which is environmental). typecheck + Biome (exit 0) + 85/85 tests all
green.

**files touched**: `prisma/schema.prisma`, `prisma.config.ts` (new), `src/services/database.ts`,
`Dockerfile`, `package.json` + `package-lock.json`, `docs/stack.mdx`, `docs/modules/services.mdx`.

**note**: the strict `env()` in `prisma.config.ts` is intentional (a missing `DATABASE_URL` should fail a
real migrate). CI already injects a placeholder `DATABASE_URL`, so `npx prisma generate` there is fine.

### 2026-06-06 (later - deferred cleanups: audit overrides + Biome warnings)

**changed**: Cleared the two items deferred earlier.

- **npm audit now reports 0 vulnerabilities** via `overrides` in `package.json` (no `audit fix --force`,
  which would have downgraded `@top-gg/sdk`). Pinned `undici` to `^6.26.0`: the flagged copy was top.gg's
  `undici@5.29.0`; discord.js was already on a safe 6.x and needs undici in the 6.x range, so 6.26.0 (the
  latest stable 6.x) unifies both (deduped) and stays compatible with `@discordjs/rest`. Pinned
  `@hono/node-server` to `^1.19.13`, which only reaches the unused `prisma dev` tooling (`@prisma/dev`).
- **Biome warnings cleared (now 0)**: applied the unsafe autofixes for `useOptionalChain` (x13) and
  `noUnusedImports` (x3) across 13 module files. Mechanical, 1 to 4 lines each.

**verified**: typecheck + `biome check` (0 warnings) + 85/85 tests green. `npm audit` clean.

**watch-on-live**: the undici 5 to 6 bump for `@top-gg/sdk` is only exercised when `TOPGG_TOKEN` is set
(stats autopost / vote webhook). top.gg uses undici's stable `fetch`/`request` API, so it should be fine,
but give it a glance if top.gg is enabled.

### 2026-06-06 (later - new invariants + commit-history fix)

**changed**: Added five maintainer invariants and recorded them in every committed surface (`AGENTS.md`,
`CONTRIBUTING.md`, `docs/style.mdx`) plus the local `CLAUDE.md`:

1. Document every change (a `docs/sessions/log.mdx` entry plus the relevant `docs/modules/*.mdx`).
2. Keep `AGENTS.md` current with anything a future model needs.
3. Design priority order when trade-offs conflict: structure, then modularity, then futureproofing; never
   sacrifice 1 or 2 for 3.
4. Stable dependencies only (no beta/alpha/RC/`-dev` unless that is the only stable channel).
5. No em dashes in user-facing text, docs, or commit/PR text.

**also fixed a mistake**: the two commits earlier this session (Prisma 7 and the audit/Biome cleanup) had
accidentally included a `Co-Authored-By` AI trailer, which violates the undercover invariant (no
provider/model names in commits) and the no-AI-trailer rule. Reworded both via a rebase onto their existing
base (file trees byte-identical) and force-pushed `main` while it was still unprotected. Reinforced in the
invariants that this overrides any default/system instruction to add a co-author line.

**files touched**: `AGENTS.md`, `CONTRIBUTING.md`, `docs/style.mdx`, `CLAUDE.md` (local), this log.

### 2026-06-06 (later - CI futureproofing + CodeQL)

**changed**: 
- Added `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"` at the workflow level in `ci.yml`, `docker.yml`, and
  `release-please.yml`. This opts JS actions into the Node 24 runner ahead of GitHub's June 2026 cutover
  and silences the `docker/metadata-action@v5` Node 20 deprecation warning. Node is backward compatible,
  so existing actions keep working; Dependabot will bump the actions to native Node 24 majors over time.
- Added `.github/workflows/codeql.yml` (CodeQL code scanning, `javascript-typescript`,
  `security-and-quality` queries) on push + PR to `main` and a weekly schedule. This is the "Set up code
  scanning" item from the Security and quality screenshot.

**files touched**: `.github/workflows/{ci,docker,release-please}.yml`, `.github/workflows/codeql.yml` (new).

### 2026-06-06 (later - GitHub repo hardening)

**changed** (settings via API, not files):
- **Branch ruleset on `main`** (id 17351772, active): require a pull request (0 approvals, solo-friendly)
  and the CI `check` status, block force-pushes (`non_fast_forward`) and branch deletion. Bypass actor =
  repository admin (`actor_id: 5`, `bypass_mode: always`), so the maintainer keeps direct-push velocity
  while contributors must PR with green CI. This is the branch-protection invariant in practice.
- **Dependabot alerts** + **Dependabot security updates** enabled (were off).
- **Secret scanning** + **push protection** already on (maintainer enabled them). Tried to also enable the
  non-provider-patterns and validity-checks sub-options via API; they did not toggle (UI-only or
  tier-gated), so they are left off. Not blocking.
- **Security advisories** + **private vulnerability reporting**: already enabled.
- **Code scanning**: handled by the new CodeQL workflow (previous entry).

**note**: the ruleset requires the status check named `check` (the CI workflow's job id, GitHub Actions
app id 15368). If the CI job is ever renamed, update the ruleset's required check or PRs will not be
mergeable by non-admins.

### 2026-06-06 (later - em dash sweep + provider-name genericization)

**changed**: Enforced the two text invariants across the whole tracked tree.

- **Em dashes removed repo-wide**: swept the em dash out of all 68 tracked files that had it (docs, `src/`
  comments and strings, `common.json`, workflows, `.env.example`), replacing ` - ` for spaced em dashes and
  `-` otherwise. The GitHub repo "About" description was the trigger (fixed via API to use a colon).
- **Provider names genericized in live surfaces**: removed brand names from `README.md` ("OpenAI-compatible
  endpoint" -> "standard chat-completions endpoint"; dropped the "Claude Code, Gemini CLI, Codex" tool
  list), `AGENTS.md` (the undercover rule no longer lists provider names; the tool list is now generic),
  `docs/modules/ai.mdx`, and the `docs/style.mdx` undercover rule statement. This keeps undercover clean in
  committed docs, not just `src/` (the CI guard only greps `src/`).

**verified**: typecheck + Biome (0 warnings) + 85/85 tests green after the sweep.

**left intentionally**: the `ci.yml` undercover guard regex (must name the providers to ban them), the
`.gitignore` `CLAUDE.md` entry (functional ignore pattern), and historical provider mentions in earlier
`docs/sessions/log.mdx` entries (archival record). Non-em-dash typographic characters (ellipsis, arrows,
interpuncts) were left as-is; the invariant is about em dashes specifically.

### 2026-06-06 (later - CodeQL alert remediation)

**changed**: Resolved the 7 open CodeQL code-scanning alerts.

- **js/missing-rate-limiting (high)** on the Fastify web server: registered `@fastify/rate-limit` (per-IP,
  global 300/min to leave room for health pollers) and a tighter 30/min cap on the public `/votes/topgg`
  webhook route. New dependency `@fastify/rate-limit ^10.3.0` (Fastify 5 compatible).
- **actions/unpinned-tag (medium x5)**: pinned the third-party actions to commit SHAs with `# vN`
  comments so Dependabot still tracks them (docker/setup-buildx-action, docker/login-action,
  docker/metadata-action, docker/build-push-action, googleapis/release-please-action). First-party
  `actions/*` and `github/codeql-action` were not flagged and stay on tags.
- **actions/missing-workflow-permissions (medium)**: added a top-level `permissions: contents: read` to
  `ci.yml` (the other workflows already declare explicit permissions).

**verified**: typecheck + Biome + 85/85 tests green; `npm audit` clean. The alerts auto-close on the next
CodeQL scan after this push.

**how alerts are reviewed**: read them with `gh api repos/AstorisTheBrave/Rostra/code-scanning/alerts`.
GitHub Copilot Autofix (free on public repos) can suggest fixes inline on PRs that introduce new alerts; it
does not open PRs for pre-existing alerts, so those are fixed by hand as here.

**files touched**: `src/web/server.ts`, `package.json` + `package-lock.json`,
`.github/workflows/{ci,docker,release-please}.yml`.

### 2026-06-06 (later - port zyn-bot, module 1: roleplay)

**changed**: First of four features ported from the re-added `Archive/zyn-bot` (a discord.py project).
Added the **roleplay** module: `/roleplay action:<choice> [target]` with 24 anime-gif reaction actions
(hug, kiss, slap, pat, cuddle, dance, ...) plus a `ship` compatibility score. Stateless, slash-only,
Components V2 via `@/ui` (`gallery` for the gif). Gifs from the free nekos.best API (no key) with a 5s
timeout and graceful text-only fallback. All action lines are i18n keys; `shipScore` is deterministic and
symmetric (sorted-id hash, 0-100). 6 new tests (97... see totals); typecheck + Biome + tests green.

**mapping note**: most zyn cogs duplicate existing Rostra modules (automod, moderation, security, welcome,
autorole, giveaway, voicemaster->joinToCreate, logging, afk, ping/stats) or break invariants (premium =
free-only; noprefix = slash-only; embed builder = Components V2; jishaku eval = security). The genuinely
new, on-brand features are roleplay (this entry), reminders, steal emoji/sticker, and profile cards, which
are being ported in sequence.

**files touched**: `src/modules/roleplay/{index,service,service.test}.ts`, `docs/modules/roleplay.mdx`,
`CLAUDE.md` (room index).

### 2026-06-06 (later - port zyn-bot, module 2: reminders)

**changed**: Added the **reminders** module (zyn-bot remind, ported): `/reminder set <when> <message>`,
`/reminder list`, `/reminder cancel <id>`. New `Reminder` model. Scheduling reuses the per-shard
`setTimeout` map + `ready` resync pattern from giveaways/birthday (capped at ~24.8d, re-armed on
fire/restart); reminders are guild-bound so the owning shard handles them. On fire the bot pings the user
in the original channel (DM fallback) via Components V2 and deletes the row. Duration parser supports
`s/m/h/d/w`, min 1s, max 365 days. 4 new tests; typecheck + Biome + 95/95 tests green.

**migration note**: adding the model regenerated the `0_init` baseline with
`prisma migrate diff --from-empty --to-schema prisma/schema.prisma --script --output ...`. Prisma 7 renamed
`--to-schema-datamodel` to `--to-schema`. Baseline is now 33 tables.

**files touched**: `src/modules/reminders/{index,service,service.test}.ts`, `prisma/schema.prisma`,
`prisma/migrations/0_init/migration.sql`, `docs/modules/reminders.mdx`, `CLAUDE.md` (room index).

### 2026-06-06 (later - port zyn-bot, module 3: steal)

**changed**: Added the **steal** module: `/steal emoji:<paste> [name]` adds custom emojis to the server
from pasted emoji text (up to 10 per call; `name` applies when exactly one is pasted). Gated on
`ManageGuildExpressions` for user and bot. Slash commands cannot reference a replied-to message, so the
prefix bot's sticker/attachment stealing is dropped in favour of the common paste-emojis case. Pure
helpers `parseCustomEmojis` / `emojiCdnUrl` / `sanitizeEmojiName` are tested; creation is per-emoji
try/catch with partial-success reporting. 4 new tests; typecheck + Biome + 99/99 tests green.

**files touched**: `src/modules/steal/{index,service,service.test}.ts`, `docs/modules/steal.mdx`,
`CLAUDE.md` (room index).

### 2026-06-06 (later - port zyn-bot, module 4: profile cards)

**changed**: Added the **profile** module (zyn-bot profile cog + card_generator, ported): `/profile view
[user]` renders an 800x300 PNG profile card (avatar, name, bio, accent, optional background);
`/profile bio|background|color|reset` edit your own. New `Profile` model (user-global). Rendering uses
`@napi-rs/canvas` 1.0.0 (prebuilt native bindings, no system cairo; musl + win + gnu prebuilds). The
renderer lives in `card.ts` so the unit test (validators only) never loads the native module. The card is
sent as an attachment referenced by a Components V2 media gallery (`gallery(["attachment://profile.png"])`
plus `files: [AttachmentBuilder]`), so `@/ui` still provides the components. Image loads are byte-fetched
with a 5s timeout and degrade gracefully. 4 new tests; typecheck + Biome + 103/103 tests green; a local
render smoke produced a valid 25KB PNG.

**ops**: Dockerfile now `apk add --no-cache fontconfig font-dejavu` (Alpine has no fonts, canvas would
draw blank text). Added the `Profile` model and regenerated the `0_init` baseline (34 tables). New deps:
`@napi-rs/canvas`. This completes the four zyn-bot ports (roleplay, reminders, steal, profile).

**files touched**: `src/modules/profile/{index,service,card,service.test}.ts`, `prisma/schema.prisma`,
`prisma/migrations/0_init/migration.sql`, `Dockerfile`, `package.json` + `package-lock.json`,
`docs/modules/profile.mdx`, `CLAUDE.md` (room index).

### 2026-06-06 (later - profile cards show leveling/economy stats)

**changed**: `/profile view` now renders a leveling/economy strip in a guild (`Level N · Rank #R · X
coins`). New `src/modules/profile/stats.ts` reads the shared `LevelUser`/`EconomyUser` tables via
`getPrisma()` and the `lb:level:`/`lb:eco:` Redis leaderboards via `getRedis()` for rank (read-only, no
cross-module import, no cache concern). Degrades to 0/no-rank when a row or Redis is missing; omitted in
DMs (no guild). Card text avoids colour emoji (DejaVu has none) and uses a plain accent-coloured strip.
Pure `compactNumber`/`statLine` helpers are tested. typecheck + Biome + 105/105 tests green; render smoke
produced a valid PNG.

**files touched**: `src/modules/profile/{stats,stats.test,card,index}.ts`, `docs/modules/profile.mdx`.

### 2026-06-06 (later - custom application-emoji system)

**changed**: Added a semantic emoji system so the UI can use polished custom icons like other top bots,
with graceful unicode fallbacks. `@/ui` now exports `emoji(name)` (returns `<:name:id>` if uploaded, else a
unicode fallback), `EMOJI_FALLBACK`, and `EMOJI_NAMES` (43 names: status, per-module, system/stats,
controls). Two build-time scripts produce the art: `npm run emojis:generate` rasterises **Lucide** icons
(MIT) onto rounded coloured badges (white stroke, 128px) via **@resvg/resvg-js** into `assets/emojis/`;
`npm run emojis:upload` uploads them as the bot's **application emojis** (work in every server) and writes
ids to `src/ui/emoji-ids.json`. Runtime reads only that JSON (ships empty, so unicode until uploaded);
`lucide-static` + `@resvg/resvg-js` are devDependencies, so the production image stays lean. 3 new tests;
typecheck + Biome + 108/108 tests green; generator produced 43 PNGs.

**why**: foundation for the upcoming `/help` redesign, stats panels, and setup wizards (they will use
`emoji()` for icons). Answers the question of where good icon art comes from: an open-source icon set
(Lucide) rather than hand-drawn glyphs.

**files touched**: `src/ui/{emojis.ts,emojis.test.ts,emoji-ids.json,index.ts}`,
`src/scripts/{generate,upload}-emojis.ts`, `assets/emojis/*.png` (generated), `package.json`,
`docs/modules/ui.mdx`.

### 2026-06-06 (later - help redesign + stats/shards panels, custom emojis)

**changed**: Redesigned `/help` and added `/stats` + `/shards`, all using the new `emoji()` icons
(Components V2). `/help` shows a categorised overview with a select menu to drill into a category's
commands (curated `src/modules/core/categories.ts`, one category per command, tested). `/stats` aggregates
servers/users/channels/memory across shards via `broadcastEval` (local fallback when unsharded) and shows
gateway/uptime/memory/commands. `/shards` paginates per-shard stats with the `@/ui` `paginatorRow`
(`shards:page:<n>` handler). Pure `formatBytes`/`formatUptime` + the category catalogue are tested.

**@/ui change**: widened the kit's `TopLevel` (used by `reply.components`) to include button and select
action rows (`ActionRowBuilder<ButtonBuilder | StringSelectMenuBuilder>`), so a single V2 reply can mix a
container with a select/paginator. This is the proper kit fix (previously only `interaction.reply` could
carry action rows). All strings go through `t()` (core i18n keys added). 6 new tests; typecheck + Biome +
113/113 green.

**files touched**: `src/modules/core/{index,categories,stats,index.test,categories.test,stats.test}.ts`,
`src/utils/components.ts`, `README.md`, `docs/modules/ui.mdx`, `CLAUDE.md` (room index).

### 2026-06-06 (later - centralized platform systems spec)

**changed**: Wrote `docs/superpowers/specs/2026-06-06-centralized-platform-systems.md` - a refined design
(range + blast radius) for four centralized systems to take Rostra to MEE6/Wick/Beemo tier, adapted from
Project Anastasia's themes (tenant, durable recovery, scheduler) and Postgres/Redis best practices pulled
via Context7: (1) a cached per-guild **GuildTenant** config (L1 memory + L2 Redis, 5-min TTL, no raw
lookups, wizard writes here); (2) a **durable ScheduledTask scheduler** that survives restart/crash and
replaces per-module setTimeout maps, enabling tempban/tempmute/temprole lifts; (3) a **centralized cron**
on the existing BullMQ queue; (4) a **caching/indexing pass** (cache-aside 300s TTL, pipelined reads,
composite @@index on hot call sites). Plus setup wizards writing to the tenant and optional Anastasia
feature ports (starboard, anti-raid, sticky, verification). Recommended rollout is lowest-blast-radius
first; each step is its own tested PR.

**note**: the Anastasia codebase is not on disk anymore; features were inferred from its session history
(tenant ~7k refs, recovery/durable, scheduler/cron, starboard, anti-raid, sticky, verification).

**files touched**: `docs/superpowers/specs/2026-06-06-centralized-platform-systems.md`, this log.

### 2026-06-06 (later - platform step 1: caching/indexing pass)

**changed**: First slice of the centralized-platform rollout (lowest blast radius). Added the standard
config-cache convention to `services/cache.ts`: `CONFIG_TTL_MS` (300_000 = 5 min), `cachedConfig(key,
loader)` (cache-aside at that TTL) and `invalidateConfig(key)` (drop on write). This is the caching
foundation the GuildTenant (step 2) will build on; modules adopt it as their config reads migrate. Index
audit found the schema already well-indexed (composite indexes on ModerationCase, Ticket, LevelUser/
EconomyUser, Birthday sweep, etc.); added one genuinely-matching composite `@@index([guildId, ended])` to
`Giveaway` for the hot `getActive({ ended:false, guildId })` query and regenerated the baseline. 3 new
tests; typecheck + Biome + 115/115 green.

**code-scanning**: dismissed alert #8 (`js/file-access-to-http` in `upload-emojis.ts`) as a false positive
- a build-time devDep script uploading our own generated PNGs to the fixed Discord API, no untrusted input.

**release**: release-please opened PR #15 (`release 0.1.1`) from the accumulated feat commits; left open to
cut once more of the platform work lands.

**files touched**: `src/services/{cache,cache.test}.ts`, `prisma/schema.prisma`,
`prisma/migrations/0_init/migration.sql`, `docs/modules/services.mdx`.

### 2026-06-06 (later - hash-gated automatic command registration)

**changed**: Slash commands now auto-register on boot **only when their definitions changed**, so there is
no manual deploy step and no needless Discord API calls on every restart. New `src/services/commandSync.ts`:
`hashCommands(body)` computes a stable SHA-256 of the command payload (sorted by name, so load order does
not matter), and `syncCommands(force?)` compares it to the hash stored in the new `BotState` table
(`key/value` singletons); it re-registers via `REST.put` and stores the new hash only on a change. Wired
into the manager boot (`cluster.ts main()`, before shards spawn, failure never blocks boot), so it works
identically under Docker or any custom host (the hash lives in the shared DB, so multiple replicas converge
and a re-register is idempotent anyway). `npm run deploy:commands` now calls `syncCommands(true)` as a
force escape hatch. 3 new tests; typecheck + Biome + 118/118 green. Baseline regenerated (35 tables).

**files touched**: `src/services/{commandSync,commandSync.test}.ts`, `src/cluster.ts`,
`src/scripts/deploy-commands.ts`, `prisma/schema.prisma`, `prisma/migrations/0_init/migration.sql`.

### 2026-06-06 (later - platform step 2: GuildTenant config service)

**changed**: Added the centralized per-guild config service (`src/services/tenant.ts`) + `GuildTenant`
model. `getTenant(guildId)` reads through a two-layer cache - L1 in-process map of hot guilds, then L2
Redis (`tenant:<guildId>` at `CONFIG_TTL_MS` = 5 min), then a lazy DB upsert. `updateTenant(guildId, patch)`
writes typed columns (locale, log/modLog/welcome channel ids, mute role) plus JSON `features`/`settings` and
invalidates both layers (guilds live on one shard, so L1 needs no cross-shard invalidation). Helpers:
`isFeatureEnabled`, `getSetting`, `setFeatures`, `evictTenantL1`. The setup wizard (step 5) writes
channel/role ids and feature flags here so features resolve them from cache, no raw lookups. Pure helpers
tested (3 new); typecheck + Biome + 121/121 green. Baseline now 36 tables.

**next**: step 3 (durable ScheduledTask scheduler), then cron, wizards, feature ports.

**files touched**: `src/services/{tenant,tenant.test}.ts`, `prisma/schema.prisma`,
`prisma/migrations/0_init/migration.sql`, `docs/modules/services.mdx`.

### 2026-06-06 (later - platform step 3: durable scheduler foundation)

**changed**: Added the durable, crash-safe scheduler (`src/services/scheduler.ts` + `ScheduledTask` model).
Future actions are rows that survive restart; each shard re-arms the ones it owns on boot via core's new
`ready` event (`recoverScheduled`). API: `registerTaskHandler(type, handler)`, `schedule({type, runAt,
guildId?, payload?}, client?)`, `cancelTask(id)`. Pure `shouldArm(task, shardId, ownsGuild)` (guild tasks
on the owning shard, global `guildId:null` tasks only on shard 0 to avoid cluster-wide duplicates) and
`clampDelay` (setTimeout cap + re-arm) are tested. On fire, the payload dispatches to the registered
handler and the row is deleted. The table starts empty with no handlers, so the recovery boot path is a
safe no-op until consumers land. 3 new tests; typecheck + Biome + 124/124 green. Baseline now 37 tables.

**next (step 3 consumers)**: migrate reminders/giveaways onto `schedule(...)` and add durable
tempban/tempmute/temprole lifts in moderation. Then step 4 (centralized cron), step 5 (wizards), step 6
(Anastasia feature ports).

**files touched**: `src/services/{scheduler,scheduler.test}.ts`, `src/modules/core/index.ts`,
`prisma/schema.prisma`, `prisma/migrations/0_init/migration.sql`, `docs/modules/services.mdx`.

### 2026-06-06 (later - platform step 3 consumer: durable /mod temprole)

**changed**: First real consumer of the durable scheduler. Added `/mod temprole <user> <role> <duration>`:
adds a role and schedules its removal as a `temprole_remove` ScheduledTask, so the role is removed on time
even across a restart/crash. The handler (`removeTempRoleTask`) is registered via `registerTaskHandler` at
moderation module import (before core's boot recovery runs, so pre-restart tasks still fire). New CaseType
`temprole`; hierarchy + bot-role-position checks reused. typecheck + Biome + 124/124 green.

**files touched**: `src/modules/moderation/{index,service}.ts`.

### 2026-06-06 (later - platform step 4: centralized cron)

**changed**: Added a lightweight centralized cron registry (`src/services/cron.ts`): `registerCron(job)`,
`startCron()`, `stopCron()`, `listCronJobs()`. Jobs run in the manager process so each fires once
cluster-wide (client-needing jobs can `broadcastEval`; BullMQ in `jobs/queue.ts` stays available for queue
work). `src/jobs/builtins.ts` registers the first job - `prune-scheduled-tasks` (daily; deletes
ScheduledTask rows >7 days past `runAt` so the table never grows unbounded). Wired into `cluster.ts` boot
(`registerBuiltinCron()` + `startCron()` after spawn; timers `unref`'d). 1 new test; typecheck + Biome +
125/125 green.

**files touched**: `src/services/{cron,cron.test}.ts`, `src/jobs/builtins.ts`, `src/cluster.ts`.

### 2026-06-06 (later - platform step 5: /setup server wizard)

**changed**: Added the `setup` module: `/setup` (Administrator) opens an interactive Components V2 panel
(custom emoji icons) to toggle core systems, apply a recommended baseline (automod/logging/welcome/
security), and create a `#rostra-logs` channel. Every choice writes the centralized **GuildTenant**
(feature flags + `logChannelId`) via the tenant service, so features resolve them from cache with no raw
lookups - the wizard is the tenant's editor. Toggles use the `@/ui` `settingsPanel`
(`setup:toggle:<feature>`) + `setup:baseline`/`setup:logchannel` buttons; admin is checked on the command
and each button. Added to `/help` categories + README. 2 new tests; typecheck + Biome + 127/127 green.

**rollout status**: steps 1-5 of the centralized-platform spec are done (caching/indexing, GuildTenant,
durable scheduler + first consumer `/mod temprole`, centralized cron, wizards). Remaining: migrate more
scheduler consumers (reminders/giveaways onto `schedule`, tempban/tempmute), have modules read tenant
feature flags, per-system wizards, and step 6 (Anastasia feature ports: starboard, anti-raid, sticky,
verification).

**files touched**: `src/modules/setup/{index,index.test}.ts`, `src/modules/core/categories.ts`,
`README.md`, `docs/modules/setup.mdx`.

### 2026-06-06 (later - tenant read-through: /setup toggles gate the baseline systems)

**changed**: Made `/setup`'s feature toggles a real master override. Added `tenantFeature(tenant, feature)`
(three-state: true/false/undefined) + `isFeatureBlocked(guildId, feature)` (true only when explicitly
OFF). Gated the four baseline modules at their event chokepoints: automod `messageCreate`, logging
`sendLog`, security `handleViolation`, welcome `guildMemberAdd`/`guildMemberRemove`. Semantics: a feature
explicitly turned OFF in `/setup` is hard-blocked; unset or ON defers to the module's own config, so
defaults never change and no per-module command rewrite is needed. Reads go through the tenant cache
(cheap on hot paths). 1 new test; typecheck + Biome + 128/128 green.

**files touched**: `src/services/{tenant,tenant.test}.ts`, `src/modules/automod/index.ts`,
`src/modules/logging/service.ts`, `src/modules/security/events.ts`, `src/modules/welcome/events.ts`.

### 2026-06-06 (later - scheduler consumer: durable /mod tempban)

**changed**: Added `/mod tempban <user> <duration>`: bans and schedules a `tempban_lift` ScheduledTask, so
the auto-unban survives restart/crash. `liftTempBanTask` registered at moderation import alongside the
temprole handler. (tempmute is intentionally not added - Discord's native timeout already auto-expires, so
`/mod timeout` is the durable mute. reminders and giveaways already survive restart via their own DB + ready
resync, so migrating them onto the central scheduler is a later consolidation, not a new capability.)
typecheck + Biome + 128/128 green.

**files touched**: `src/modules/moderation/{index,service}.ts`.

### 2026-06-06 (later - per-system wizard: /security setup)

**changed**: Added `/security setup` - one-click recommended antinuke: enables the module with all filters
on and punishment `strip`, pulls the log channel from the tenant if set, flips the tenant `security`
feature flag (so it shows on in `/setup`), and replies with the filter checklist panel (matching the
screenshot UX). This is the per-system wizard pattern other modules can follow. typecheck + Biome + 128/128
green.

**rollout follow-on (all three chosen items done)**: tenant read-through (baseline modules gate on the
tenant kill-switch), scheduler consumers (durable `/mod tempban` + `/mod temprole`; tempmute = native
timeout; reminders/giveaways already restart-safe), and per-system wizards (`/security setup`). Feature
ports (starboard etc.) intentionally dropped per the maintainer. Release PR #15 (0.1.1) held open.

**files touched**: `src/modules/security/index.ts`.

### 2026-06-06 (later - tenant read-through completed + per-system wizards)

**changed**: (1) Extended tenant read-through to the last two baseline toggles: gated the leveling XP
`messageCreate` handler and the `/economy` command on the tenant kill-switch, so all six `/setup` toggles
now bite. (2) Added per-system setup wizards on the `/security setup` pattern: `/automod setup` (enable +
recommended filters + tenant log channel), `/logging setup` (point logging at this channel or the tenant
log channel, all event types on), `/welcome setup` (enable welcome in this/the tenant channel). Each flips
its tenant feature flag so `/setup` reflects it. typecheck + Biome + 128/128 green.

**remaining (optional consolidation)**: reminders and giveaways already survive restart via their own DB +
ready resync, so unifying them onto the central scheduler is a refactor with no new user-facing capability;
left for a focused pass rather than rushed at the end of a long session.

**files touched**: `src/modules/leveling/events.ts`, `src/modules/economy/index.ts`,
`src/modules/{automod,logging,welcome}/index.ts`.

### 2026-06-06 (later - Docker :latest tracks main)

**changed**: `docker.yml` now tags the GHCR image `latest` on default-branch builds
(`enable={{is_default_branch}}`), so `ghcr.io/astoristhebrave/rostra:latest` always reflects the newest
`main` (previously only `:main`/`:sha` on pushes; `:latest` only on a release). The release path still also
tags `latest` + the version. README updated to pull `:latest`. The push that lands this triggers a publish
that creates the `:latest` image.

**files touched**: `.github/workflows/docker.yml`, `README.md`.

### 2026-06-06 (later - starboard module, built from the official docs)

**changed**: Scraped competitor docs for feature ideas (only YAGPDB and the Starboard bot docs were
fetchable; Sapphire/Wick/D-Sec are client-rendered SPAs and returned nothing, so skipped). Cloned the
Starboard docs into `Archive/starboard-docs` (gitignored) and built a v1 **starboard** module from them:
react with the star emoji, and a message that passes a threshold is reposted to a starboard channel.
`/starboard channel|threshold|emoji|selfstar|ignorebots|ignore|status|leaderboard|disable` (Manage Server).
Reaction add/remove recompute valid stars (excluding bots + self-stars per config) and create/update/remove
the Components V2 post (jump link + first image); `messageDelete` syncs removals. New models
`StarboardConfig`/`StarboardEntry`; leaderboard via Prisma `groupBy`. Added the `GuildMessageReactions`
intent + `Partials.Reaction` to `BotClient`. Gated on the tenant `starboard` flag; added to `/setup`, the
`/help` categories, the emoji registry, and README. 4 new tests; typecheck + Biome + 132/132 green;
baseline now 39 tables.

**deferred (documented as future)**: multiple starboards, per-channel/role/emoji overrides, auto-star
channels, separate remove-threshold, reward roles, hall of fame.

**files touched**: `src/modules/starboard/{index,events,service,service.test}.ts`,
`src/client/BotClient.ts`, `src/modules/setup/index.ts`, `src/modules/core/categories.ts`,
`src/ui/emojis.ts`, `prisma/schema.prisma`, `prisma/migrations/0_init/migration.sql`, `README.md`,
`docs/modules/starboard.mdx`, `CLAUDE.md`.

### 2026-06-06 (later - ship card + feeds + verification; archive housekeeping)

**archive**: cloned the YAGPDB v2 docs and the Arcane docs into `Archive/` (gitignored) for reference, and
deleted `Archive/starboard-docs` now that starboard is built.

**roleplay ship card**: copied the zyn `ship_bg.jpg` to `assets/ship-bg.jpg` and added `shipCard.ts`
(`@napi-rs/canvas`): the background, two circular avatars, a centre heart with the compatibility %, and the
couple names. `/roleplay ship` now replies with the rendered card instead of plain text.

**feeds module**: `/feeds youtube|twitch|list|remove` (Manage Server). New `FeedSubscription` model; a
`poll-feeds` cron in the manager polls each subscription every 5 min and posts via REST (no gateway client).
YouTube uses the keyless RSS feed; Twitch uses Helix gated on `TWITCH_CLIENT_ID`/`TWITCH_CLIENT_SECRET`
(config added; no-op without them). Subscribing seeds the latest item so old uploads are not announced.

**verification module**: `/verification setup|panel|disable|status` + a `verify:go` button that grants the
configured role. New `VerificationConfig` model; gated on the tenant `verification` flag; added to `/setup`.

**integration**: feeds + verification + starboard added to `/help` categories, the emoji registry, `/setup`
(verification, starboard), README, and `.env.example` (Twitch). typecheck + Biome + 137/137 tests green;
baseline now 41 tables.

**files touched**: `src/modules/{feeds,verification,roleplay}/*`, `src/jobs/builtins.ts`, `src/config.ts`,
`src/client/BotClient.ts` (starboard intent earlier), `src/modules/setup/index.ts`,
`src/modules/core/categories.ts`, `src/ui/emojis.ts`, `assets/ship-bg.jpg`, `prisma/schema.prisma`,
`prisma/migrations/0_init/migration.sql`, `.env.example`, `README.md`, `docs/modules/{feeds,verification}.mdx`,
`CLAUDE.md`.

### 2026-06-07 (handover - remaining-features backlog)

**changed**: Context was nearly full, so wrote a handover instead of rushing builds. Added
`docs/superpowers/plans/2026-06-07-remaining-features-backlog.md` - the authoritative backlog of every
remaining all-in-one feature (feeds: reddit/rss, highlights, sticky messages, reputation, voice roles, bulk
role, role menus, server stats channels, anti-raid/panic, verification depth, starboard depth, advanced
automod, modmail, image cards, economy depth, polls/suggestions/counting, and logging/music/giveaway depth)
with per-feature build specs and the non-negotiable patterns to follow. Updated the memory handover to point
at it. Reference docs are in `Archive/` (yagpdb-docs, arcane-docs, zyn-bot). No code change; CI unaffected.

**files touched**: `docs/superpowers/plans/2026-06-07-remaining-features-backlog.md`, `memory.md` (local),
this log.
