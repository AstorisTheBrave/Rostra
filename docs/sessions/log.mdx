---
last-updated: 2026-06-05
owner: AstorisTheBrave
status: stable
---

## what this is

The running history of structural changes to Rostra, one entry per working session. Read the latest entry
to see where things stand and what was decided.

## log

### 2026-06-05

**changed**: Project created. Phase 1 audit of 15 archived bots completed. Architecture designed and
approved (single modular TS package; Postgres+Prisma+Redis; native ShardingManager with hybrid switch;
slash-only + Components V2; no dashboard; no premium; undercover mode). Private repo created. **Plan 1a
(Foundation) implemented and merged to `main`**: config loader, Pino logger, Prisma core schema +
`getPrisma`, Redis/Keyv cache, Components V2 kit, i18n, sharding IPC abstraction. 14/14 tests pass;
typecheck + Biome clean. top.gg + multi-node Lavalink folded into the spec; 10 Lavalink nodes stored in
local `.env`. Documentary docs system set up.

**files touched**: `package.json`, `tsconfig.json`, `tsup.config.ts`, `biome.json`, `.env.example`,
`prisma/schema.prisma`, `src/config.ts`, `src/services/{logger,database,cache}.ts`,
`src/utils/components.ts`, `src/i18n/index.ts` + `locales/en/common.json`, `src/cluster/ipc.ts`,
`src/types/global.d.ts` (+ matching `*.test.ts`).

**docs updated**: created `docs/overview.mdx`, `stack.mdx`, `style.mdx`, `architecture/index.mdx`,
`modules/{config,services,components,i18n,ipc}.mdx`, `infrastructure/{env,deployment}.mdx`, this log;
specs/plans under `docs/superpowers/`.

**decisions made**: Postgres over Turso (write-heavy at 1M DAU). Slash-only. Components V2 only. No
premium (all free). Undercover mode is a hard invariant (no AI-provider references). Stats autopost from
the manager process, not per-shard. Bot name = "Rostra".

**next**: Plan 1b (Runtime) — DONE (see next entry). Then feature modules (moderation first).

### 2026-06-05 (later — Plan 1b Runtime)

**changed**: Implemented the full runtime layer. Module contract (`SlashCommand`/`ComponentHandler`/
`RegisteredEvent`/`JobDefinition`/`BotModule`), `BotClient` with auto-loaders, `defineEvent` typed
factory, command pipeline (permissions → cooldown → error boundary), interaction router, BullMQ job
queue (in-memory fallback), top.gg service + Fastify health/metrics + `/votes/topgg` webhook + stats
autoposter, `cluster.ts` (ShardingManager) + `bot.ts` shard entry, graceful shutdown, deploy-commands,
sample `core` module (`/ping`, `/help`), Dockerfile + docker-compose + README + CI. **Live login verified**
(connected as Felisha#3245). 33/33 tests pass, typecheck + lint clean, embed guard clean.

**decisions made**: Run via **tsx in dev AND prod** (dropped tsup) — bundling collapses `import.meta.url`
which breaks filesystem module discovery; running TS directly keeps drop-in modules working. Renamed env
`SHARD_COUNT`→`TOTAL_SHARDS` to avoid colliding with discord.js's reserved sharding env var. top.gg stats
posted from the manager process (aggregate across shards). Removed unused `tsup`/`topgg-autoposter` deps;
moved `tsx` to runtime deps.

**files touched**: `src/types/module.ts`, `src/client/{BotClient,defineEvent}.ts`,
`src/client/loaders/*`, `src/pipeline/*`, `src/interactions/router.ts`, `src/jobs/queue.ts`,
`src/services/topgg.ts`, `src/web/{server,autopost}.ts`, `src/lifecycle/shutdown.ts`,
`src/modules/core/index.ts`, `src/scripts/deploy-commands.ts`, `src/{cluster,bot}.ts`, `Dockerfile`,
`docker-compose.yml`, `README.md`, `.github/workflows/ci.yml`, `package.json` (+ tests).

**next**: First feature module — moderation (DONE, see next entry).

### 2026-06-05 (later — moderation module)

**changed**: Built the first feature module. `/mod` with 15 subcommands (ban, unban, kick, timeout,
untimeout, warn, warnings, removewarn, cases, note, purge, slowmode, lock, unlock, nick). New Prisma
models `ModerationCase` (warnings/notes share it via `type`) + `GuildCaseCounter` (per-guild sequential
case numbers via a transactional counter). Service ports TitanBot's hierarchy/bot-hierarchy guards and
`kickable`/`moderatable` checks into TS, persisting cases relationally (vs TitanBot's KV). Duration
parser (`10m`/`1h30m`/`2d`) with tests. All replies Components V2; all strings `moderation:*` i18n keys;
per-subcommand permission checks. 37/37 tests pass, typecheck + lint + embed guard clean; loader shows
`help, mod, ping`.

**files touched**: `src/modules/moderation/{index,service,duration}.ts` (+ `duration.test.ts`),
`prisma/schema.prisma`, `docs/modules/moderation.mdx`.

**decisions made**: Unify warnings/notes/infractions in one `ModerationCase` table keyed by `type`.
Per-guild case numbers from a `GuildCaseCounter` row incremented in the same transaction as the insert.
Soft-delete cases (`active=false`), never hard-delete. `/mod` uses one top-level command + subcommands
(stays well under the 100-command cap) with `ModerateMembers` default visibility + per-subcommand perm
checks.

**next**: see utility entry below, then security/antinuke.

### 2026-06-05 (later — utility module + V2 kit extension)

**changed**: Added the **utility** module: `/util avatar|banner|userinfo|serverinfo` (read-only, no DB).
Extended the Components V2 kit (`src/utils/components.ts`) with `gallery()` (media gallery from image
URLs) and `section()` (text block with thumbnail accessory), widening the container/top-level unions to
accept them. 39/39 tests pass; typecheck + lint + embed guard clean; loader shows `help, mod, ping, util`.

**files touched**: `src/modules/utility/index.ts`, `src/utils/components.ts` (+ `components.test.ts`),
`docs/modules/utility.mdx`.

**decisions made**: `banner` force-fetches the user (banners aren't on the cached partial). Timestamps use
Discord `<t:unix:F|R>` markup. The `gallery`/`section` helpers are the reusable path for any module that
needs images — modules must not hand-roll V2 builders.

**next**: security/antinuke (DONE — see next entry).

### 2026-06-05 (later — security/antinuke module)

**changed**: Built the antinuke module. `/security` (Admin-only) config command: enable/disable/status,
punishment (ban|kick|strip), logchannel, notify, per-module toggle, and whitelist/extraowner add/remove
groups. 9 detection events (channelCreate/Delete, roleCreate/Delete, guildBanAdd, guildMemberRemove,
guildMemberAdd-bots, webhooksUpdate, guildUpdate) attribute the actor via audit log (`resolveAuditExecutor`,
ported from i-karen with retry + TTL) and `punish()` the untrusted executor (hierarchy-checked, with
strip-roles fallback) + log + owner DM. New `AntinukeConfig` model with `String[]` whitelist/extraOwners.
Config cached per-process. 41/41 tests pass; typecheck + lint + embed guard clean; loader registers
`help, mod, ping, security, util`.

**files touched**: `src/modules/security/{index,events,audit,service}.ts` (+ `service.test.ts`),
`prisma/schema.prisma`, `docs/modules/security.mdx`.

**decisions made**: Immediate-punish-on-unauthorized-action (core antinuke) rather than i-karen's
threshold/panic system (deferred). Single trusted whitelist + extraOwners (vs per-action whitelist).
v1 does not auto-revert deleted resources. Antinuke config in its own table, not the generic
`GuildModuleConfig`, for typed per-field access in hot event paths.

**next**: automod (DONE — see next entry).

### 2026-06-05 (later — automod module)

**changed**: Built automod. `/automod` (Manage Server) config: enable/disable/status, action
(delete|warn|timeout), per-filter toggle, limits, logchannel, exempt role/channel groups, custom-word
groups. messageCreate guard runs `checkMessage` (invite, link w/ allow-list, mass-mention, caps,
profanity via `bad-words`, spam sliding-window) and `enforce` (delete + optional timeout + log). New
`AutomodConfig` model; config + per-guild `bad-words` Filter cached. Pure detectors in `checks.ts` with
tests. 44/44 tests pass; typecheck + lint + embed guard clean; loader registers `automod, help, mod,
ping, security, util`.

**files touched**: `src/modules/automod/{index,service,checks}.ts` (+ `checks.test.ts`),
`prisma/schema.prisma`, `docs/modules/automod.mdx`.

**decisions made**: Used the `bad-words` dependency (per project requirement) as the profanity engine,
wrapped with a per-guild cached Filter + custom words. Spam tracking is in-memory per-process (correct
since a guild's traffic lands on one shard). Fixed check order; first hit wins; exempt roles/channels
short-circuit.

**next**: logging (DONE — see next entry).

### 2026-06-05 (later — logging module)

**changed**: Built audit logging. `/logging` (Manage Server): channel, toggle <type>, status, ignore
add/remove group. 10 event handlers (message delete/edit, member join/leave/ban/unban, role create/delete,
channel create/delete) → `sendLog` posts a Components V2 entry to the configured channel when the type is
enabled. New `LoggingConfig` model; cached. `truncate` helper tested. 46/46 tests pass; typecheck + lint
+ embed guard clean; loader registers `automod, help, logging, mod, ping, security, util`.

**files touched**: `src/modules/logging/{index,events,service}.ts` (+ `service.test.ts`),
`prisma/schema.prisma`, `docs/modules/logging.mdx`.

**decisions made**: `sendLog` takes a builder thunk (container only built when the event is enabled).
Message events skip bots + ignored channels; partials show "—" for missing content.

**next**: **welcome/autorole** module — welcome/goodbye messages to a channel + auto-role on join
(per-guild config). Then tickets, applications, economy, leveling, giveaways, etc. per build order.
